Отлично! Мы продолжим наше профессиональное обучение Dart.

В **Уроке 1** мы охватили основы: что такое Dart, как он компилируется (AOT, JIT, JS), структуру программы `main()` и основные типы данных.

Теперь мы углубимся в **Урок 2: Неизменяемость, Нулевая Безопасность (Null Safety) и Управляющие Конструкции**. Эти темы критически важны для написания надежного и современного Dart-кода.

---

## 📚 Урок 2: Неизменяемость, Null Safety и Управляющие Конструкции

### 1. Неизменяемость (Immutability) в Dart

Профессиональный код часто стремится к неизменяемости, так как это упрощает отладку и предотвращает неожиданные побочные эффекты, особенно в асинхронном или многопоточном коде.

Dart предоставляет два ключевых слова для объявления переменных, чьи значения не могут быть изменены после инициализации: **`final`** и **`const`**.

| Ключевое Слово | Когда Инициализируется                 | Уровень Изменяемости                                                                                                | Подробности "Под Капотом"                                                                                                        |
| -------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| **`final`**    | **Во время выполнения** (Runtime)      | Переменная должна быть инициализирована только один раз, но значение может быть вычислено в момент обращения к ней. | Используется для данных, которые не изменятся после получения (например, данные из API, текущее время).                          |
| **`const`**    | **Во время компиляции** (Compile-time) | Переменная и ее значение должны быть **абсолютно** неизменными и известными до запуска программы.                   | Обеспечивает максимальную производительность, так как компилятор может кэшировать и повторно использовать одно и то же значение. |

#### Примеры Использования:

```dart
void main() {
  // 1. const (Константа времени компиляции)
  const double pi = 3.14159;
  // pi = 3.14; // ОШИБКА: Попытка присвоить новое значение 'const' переменной

  // 2. final (Константа времени выполнения)
  final DateTime now = DateTime.now(); // Время вычисляется в момент запуска (runtime)
  // now = DateTime.now(); // ОШИБКА: Нельзя переприсвоить 'final' переменную

  // const в списках:
  const List<int> compileTimeList = [1, 2, 3];
  // compileTimeList.add(4); // ОШИБКА: Список, объявленный как const, сам по себе неизменяем (immutable).

  // final в списках:
  final List<int> runtimeList = [1, 2, 3];
  runtimeList.add(4); // РАЗРЕШЕНО: 'final' означает, что нельзя изменить саму ПЕРЕМЕННУЮ runtimeList,
                      // но можно изменить ЕЕ СОДЕРЖИМОЕ, если это изменяемый объект (List).
                      // (Это важный нюанс!)
}

```

> **Совет для собеседования:** Если вас спросят о разнице между `final` и `const`, главный ответ — **время инициализации** (`const` — compile-time, `final` — runtime).

### 2. Нулевая Безопасность (Null Safety)

Начиная с Dart 2.12, была введена **Нулевая Безопасность** (Sound Null Safety). Это одна из самых важных функций Dart, которая практически устраняет знаменитые ошибки `NullPointerException` (или `NoSuchMethodError` в Dart).

**Главное правило:** По умолчанию, **все переменные в Dart являются не-нулевыми** (Non-Nullable).

#### А. Не-нулевые Типы (Non-Nullable Types)

Переменная не может содержать значение `null`, если только вы явно этого не разрешите.

```dart
String name = 'Alice';
// name = null; // ОШИБКА КОМПИЛЯЦИИ: Non-nullable variable 'name' can't be assigned null.

int count;
// print(count); // ОШИБКА: Non-nullable local variable 'count' must be assigned before it can be used.

```

#### Б. Нулевые Типы (Nullable Types)

Чтобы разрешить переменной принимать `null`, вы должны добавить знак вопроса (**`?`**) к ее типу.

```dart
String? optionalName = null; // Разрешено
optionalName = 'Bob'; // Разрешено

int? nullableCount; // По умолчанию null, если не инициализировано
nullableCount = 5;

```

#### В. Операторы Для Работы с Nullable

Для безопасной работы с нулевыми типами используются следующие операторы:

| Оператор  | Название                             | Пример                                      | Объяснение                                                                                                                                                                                       |
| --------- | ------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`?.`**  | Null-Aware Access                    | `String? a = null; print(a?.length);`       | Вызывает метод/получает свойство только если объект **не** `null`. Возвращает `null`, если объект `null`. (Вывод: `null`)                                                                        |
| **`??`**  | Null Coalescing                      | `int? b = null; int result = b ?? 0;`       | Если `b` **не** `null`, возвращает `b`. Иначе, возвращает правое значение (`0`). (Вывод: `0`)                                                                                                    |
| **`??=`** | Null Coalescing Assignment           | `String? c; c ??= 'Default';`               | Присваивает правое значение только если переменная в данный момент **`null`**. (Вывод: `'Default'`)                                                                                              |
| **`!`**   | Null Assertion (The "Dare" Operator) | `String guaranteedNonNull = optionalName!;` | **Оператор утверждения.** Сообщает компилятору: "Я гарантирую, что здесь нет `null`". **ОПАСНО:** Если значение _все-таки_ `null` во время выполнения, это вызовет исключение (`Runtime Error`). |

> **Профессиональный совет:** Избегайте использования `!` (оператора "Дерзости"), если вы не можете _абсолютно_ гарантировать, что значение не будет `null`. Использование `!` игнорирует Null Safety и может привести к сбоям. Предпочитайте `?.` или `??`.

### 3. Управляющие Конструкции (Control Flow)

Это стандартные конструкции, позволяющие программе принимать решения и повторять действия.

#### А. Условные Операторы (Decisions)

1. **`if-else if-else`**: Стандартное ветвление логики.

```dart
int score = 85;
if (score >= 90) {
  print('Отлично');
} else if (score >= 80) {
  print('Хорошо');
} else {
  print('Удовлетворительно');
}

```

2. **`switch`**: Позволяет сравнивать значение переменной с несколькими _целочисленными, строковыми или константными_ значениями.

```dart
String command = 'OPEN';
switch (command) {
  case 'OPEN':
    print('Файл открыт.');
    // Dart не требует break; если блок case пуст.
    break; // break обязателен, чтобы избежать "проваливания" (fall-through)
  case 'CLOSE':
  case 'EXIT': // Можно объединять case-ы, если у них одинаковый код
    print('Закрытие системы.');
    break;
  default:
    print('Неизвестная команда.');
}

```

> **Нюанс Dart:** В отличие от C/C++ или Java, в Dart `switch` **требует** явного `break` (или `return`, `throw`) для предотвращения **fall-through** (проваливания) в следующий `case`, если только этот следующий `case` не помечен как пустой.

#### Б. Циклы (Loops)

1. **`for` (Классический)**: Используется для итерации фиксированное количество раз.

```dart
for (int i = 0; i < 3; i++) {
  print('Итерация $i');
}

```

2. **`for-in` (Итерация по коллекции)**: Используется для прохода по всем элементам итерируемого объекта (List, Set, Map keys и т.д.).

```dart
List<String> names = ['Anna', 'Ben', 'Chris'];
for (final name in names) { // Часто используйте final здесь для лучшей практики
  print('Имя: $name');
}

```

3. **`while` и `do-while**`:

- `while`: Выполняет блок кода, пока условие истинно. (Проверка условия _до_ выполнения)
- `do-while`: Выполняет блок кода хотя бы один раз, а затем проверяет условие. (Проверка условия _после_ выполнения)

```dart
int counter = 0;
while (counter < 3) {
  print('While: $counter');
  counter++;
}

int j = 0;
do {
  print('Do-While: $j');
  j++;
} while (j < 0); // Выполнится один раз, даже если j < 0 ложно.

```

---

## 🚀 Ваше Задание на Урок 2

1. Создайте переменные `final List<String> immutableList = ['A', 'B'];` и `const int compileTimeValue = 100;`. Попробуйте изменить их (например, `immutableList.add('C');` и `compileTimeValue = 200;`). Объясните, почему одна попытка удачна, а другая нет.
2. Объявите нулевую строковую переменную: `String? userName;`. Используйте оператор **`??`** для вывода имени пользователя или строки `'Гость'`, если `userName` равно `null`.
3. Напишите программу с функцией `switch`, которая принимает число от 1 до 5 и выводит его название (`'Один'`, `'Два'` и т.д.). Используйте `default` для всех остальных чисел.
4. Используйте цикл `for-in` для перебора и вывода всех ключей и значений в следующей Map: `Map<String, int?> stock = {'Apple': 150, 'Google': 2500, 'Tesla': null};`. При этом используйте `??` для вывода значения 'Нет данных', если значение равно `null`.

Мы проделали огромную работу по усвоению основ безопасности и неизменяемости. Готовы перейти к **Уроку 3: Функции, Дополнительные Параметры и Лямбды**?
