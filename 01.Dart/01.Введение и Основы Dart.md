## 📚 Урок 1: Введение и Основы Dart

### 1. Что Такое Dart?

Dart — это клиентский оптимизированный язык программирования, разработанный Google.

- **Назначение:** Изначально Dart создавался как замена JavaScript для веб-разработки. Однако его популярность значительно возросла с появлением фреймворка **Flutter** (для кросс-платформенной разработки мобильных, веб- и настольных приложений).
- **Типизация:** Dart является языком со **статической типизацией** (но с поддержкой вывода типов), что означает, что типы переменных проверяются во время компиляции. Это помогает выявлять ошибки на ранних этапах.
- **Парадигмы:** Dart поддерживает несколько парадигм программирования:
- **Объектно-Ориентированное Программирование (ООП):** Все в Dart является объектом, даже примитивные типы (например, числа, логические значения).
- **Функциональное Программирование:** Поддерживает функции первого класса (функции могут быть переданы как аргументы или возвращены из других функций).
- **Императивное/Процедурное Программирование.**

- **Компиляция "Под Капотом":**
- Dart может компилироваться в **нативный код** (с помощью компилятора **Ahead-Of-Time, AOT**) для максимально быстрого запуска на мобильных и настольных устройствах.
- Dart может компилироваться в **JavaScript** для веб-приложений.
- Dart использует **Just-In-Time (JIT) компиляцию** во время разработки (особенно с Flutter) для функции **Hot Reload** (мгновенное обновление кода без перезапуска приложения).

### 2. Установка Dart SDK

Прежде чем писать код, необходимо установить **Dart Software Development Kit (SDK)**.

1. **Загрузка:** Загрузите SDK с официального сайта Dart.
2. **Настройка PATH:** Убедитесь, что путь к исполняемым файлам Dart (например, `dart`, `pub`) добавлен в переменную среды `PATH` вашей операционной системы.
3. **Проверка:** Откройте терминал и введите команду:

```bash
dart --version

```

Если вы увидите номер версии, установка прошла успешно.

### 3. Инструменты Разработки (IDE)

Для профессиональной разработки рекомендуются:

- **VS Code:** Самый популярный выбор. Установите расширение **Dart** (для подсветки синтаксиса, автодополнения и отладки) и, если будете использовать Flutter, расширение **Flutter**.
- **Android Studio/IntelliJ IDEA:** Также имеют отличную поддержку Dart/Flutter с мощными инструментами для рефакторинга.

### 4. Первая Программа Dart

Программы Dart используют **точки входа** (entry points).

#### А. Основная Структура

Все исполняемые программы Dart должны содержать функцию **`main()`**. Эта функция является точкой, с которой начинается выполнение программы.

```dart
// 1. Импорт - пока пропустим, но для большинства программ он нужен.
// import 'dart:core'; // Фактически импортируется по умолчанию

// 2. Функция main() - точка входа в программу.
void main() {
  // 3. Выполнение кода
  print('Привет, мир Dart!');
}

```

- **`void`:** Ключевое слово, указывающее, что функция `main()` **ничего не возвращает**.
- **`main()`:** Имя функции.
- **`{ ... }`:** Тело функции, где находится исполняемый код.
- **`print()`:** Встроенная функция Dart, которая выводит текст в консоль (или лог).
- **`;` (Точка с запятой):** Dart требует точку с запятой после каждого оператора (инструкции), за исключением некоторых структур, таких как объявления функций или блоков кода.

#### Б. Запуск Кода

1. **Сохранение:** Сохраните код в файле, например, `hello.dart`.
2. **Выполнение:** В терминале введите команду:

```bash
dart run hello.dart
// Вывод: Привет, мир Dart!

```

### 5. Ключевые Понятия: Переменные и Типы Данных

Переменные — это именованные области памяти, которые хранят данные.

#### А. Объявление Переменных

Вы можете объявить переменную двумя основными способами:

1. **Явное указание типа (Статическая Типизация):**

```dart
String name = 'Алекс';
int age = 30;
double height = 1.85;
bool isStudent = false;

```

- Это явный и предпочтительный способ, поскольку он делает код более читаемым и помогает компилятору.

2. **Использование `var` (Вывод Типа):**

```dart
var city = 'Таллинн'; // Компилятор Dart "догадывается", что это String
var population = 430000; // Компилятор "догадывается", что это int

```

- Ключевое слово **`var`** указывает Dart автоматически определить (вывести) тип переменной на основе присвоенного ей значения **в момент инициализации**. **Важно:** После этого тип _не может_ быть изменен.

#### Б. Встроенные Типы Данных (Built-in Types)

Dart имеет следующие основные встроенные типы:

| Тип          | Назначение                                           | Примеры                                 | Подробности "Под Капотом"                                                                                                                |
| ------------ | ---------------------------------------------------- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| **`int`**    | Целые числа (до 64 бит на платформе Dart VM)         | `10`, `-500`, `0`                       | Представляет собой объект типа `Int` (наследуется от `num` и, в конечном итоге, от `Object`).                                            |
| **`double`** | Числа с плавающей точкой (64-битный формат IEEE 754) | `3.14`, `-0.001`, `1.0`                 | Представляет собой объект типа `Double` (наследуется от `num` и `Object`).                                                               |
| **`num`**    | Родительский класс для `int` и `double`.             | `num x = 12;` `num y = 1.5;`            | Удобен, когда переменная может содержать либо целое, либо дробное число.                                                                 |
| **`bool`**   | Логические значения                                  | `true`, `false`                         | Используется только для логических значений, **нет автоматического преобразования** (как в C++/JS) 0 в `false`, а других чисел в `true`. |
| **`String`** | Последовательность символов Unicode (UTF-16)         | `'hello'`, `"Dart"`, `'''multi-line'''` | Является неизменяемым (immutable) объектом. Объявляется одинарными или двойными кавычками.                                               |
| **`List`**   | Упорядоченная коллекция объектов (массив)            | `[1, 2, 3]`, `['a', 'b']`               | Называется в Dart **списком**.                                                                                                           |
| **`Map`**    | Коллекция пар ключ-значение                          | `{'key': 'value', 1: 'one'}`            | Похож на словари в Python или объекты в JS.                                                                                              |
| **`Runes`**  | Представление кодовых точек Unicode                  |                                         | Редко используется напрямую, в основном для работы с символами Unicode, отличными от базового многоязычного плана.                       |
| **`Symbol`** | Непрозрачный идентификатор                           | `#my_symbol`                            | Используется для метапрограммирования и рефлексии (редко используется в повседневном коде).                                              |

---

### 💡 Понимание "Под Капотом": "Всё есть Объект"

**Ключевой факт Dart:** _Всё, что вы можете присвоить переменной, является объектом, и все объекты наследуются от класса `Object`._

Это означает, что даже `int` и `bool` имеют методы!

```dart
int number = 10;
String text = number.toString(); // Используем метод объекта int

print(text.runtimeType); // Выводит "String"
print(number.runtimeType); // Выводит "int"

```

В отличие от многих других языков, где примитивы (например, `int`) не являются объектами, в Dart они являются **боксированными** (boxed) объектами, что унифицирует работу с типами.

---

## 🚀 Ваше Задание на Урок 1

1. **Установите Dart SDK** и проверьте его версию.
2. Создайте файл `my_first_dart_app.dart`.
3. Напишите программу, которая объявляет переменные всех основных типов (`int`, `double`, `bool`, `String`) используя **явную типизацию** и выводит их значения и типы (`.runtimeType`) в консоль.
4. Сравните результат с использованием `var`.

**Пример для задания:**

```dart
void main() {
  // Ваш код здесь
  int myInteger = 42;
  print('Значение: $myInteger, Тип: ${myInteger.runtimeType}');
}

```

Как только вы справитесь с этим, мы перейдем к **Уроку 2: Управляющие Конструкции, Неизменяемость и Null Safety**.

С чего бы вы хотели начать: с установки или сразу перейти к написанию первой программы?
