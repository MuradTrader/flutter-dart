Отлично! Переходим к **Уроку 3**, который посвящен самому важному строительному блоку любой программы — **функциям**. Мы рассмотрим, как Dart обрабатывает функции, их различные типы параметров и мощные возможности функционального программирования.

---

## 📚 Урок 3: Функции, Параметры и Функциональные Конструкции

### 1. Основы Функций (Functions)

Функция — это именованный блок кода, который выполняет определенную задачу и может быть вызван многократно.

#### А. Объявление и Возвращаемый Тип

В Dart функции могут возвращать значение, и вы должны указывать его тип перед именем функции. Если функция ничего не возвращает, используйте ключевое слово **`void`**.

```dart
// 1. Функция с явным возвращаемым типом
double calculateArea(double length, double width) {
  return length * width;
}

// 2. Функция, которая ничего не возвращает
void greet(String name) {
  print('Привет, $name!');
}

// 3. Сокращенный синтаксис (Fat Arrow/Expression Body)
// Используется, когда тело функции состоит из одного выражения.
int add(int a, int b) => a + b;

```

> **Под Капотом (Expression Body):** Сокращенный синтаксис (`=> expression;`) — это просто сахар. Компилятор Dart (и Flutter's Hot Reload) очень эффективно обрабатывает такие функции, поскольку они гарантированно возвращают результат одного выражения, что улучшает читаемость и производительность небольших функций.

### 2. Типы Параметров Функций

В Dart есть четыре основных способа определения параметров, что дает разработчику огромную гибкость.

#### А. Обязательные Позиционные Параметры (Required Positional)

Это самые простые параметры. Они должны быть переданы в том порядке, в котором они объявлены, и они обязательны.

```dart
// 'text' и 'count' обязательны и позиционны.
void repeatMessage(String text, int count) {
  for (int i = 0; i < count; i++) {
    print(text);
  }
}
// Вызов: repeatMessage('Hello', 3);

```

#### Б. Необязательные Позиционные Параметры (Optional Positional)

Они объявляются в квадратных скобках **`[]`** и всегда должны идти после обязательных параметров.

```dart
void log(String message, [String? tag]) { // tag необязателен и может быть null
  String output = tag != null ? '[$tag] $message' : message;
  print(output);
}
// Вызовы:
log('Системная ошибка');         // tag = null
log('Запуск программы', 'INFO'); // tag = 'INFO'

```

#### В. Именованные Параметры (Named Parameters)

Это краеугольный камень функций в Dart, особенно во Flutter. Они объявляются в фигурных скобках **`{}`**.

1. **Порядок не имеет значения:** Вызывающий код указывает имя параметра, делая вызов более понятным.
2. **По умолчанию они необязательны и нулевые:**

```dart
void configure(
    {int? timeout, bool? enableLogging, String? deviceId}) {
  print('Таймаут: ${timeout ?? 30}'); // Используем ?? для дефолтного значения
  // ...
}
// Вызовы:
configure(enableLogging: true, deviceId: 'A123'); // Порядок изменен, timeout = null
configure(timeout: 60);

```

#### Г. Обязательные Именованные Параметры (`required`)

Если вы хотите, чтобы именованный параметр был обязателен, используйте ключевое слово **`required`** перед типом.

```dart
// Эту функцию невозможно вызвать без явного указания title и body
void createPost({
  required String title,
  required String body,
  String? author, // author по-прежнему необязателен
}) {
  print('Создан пост: $title');
}
// Вызов:
createPost(title: 'Новость', body: 'Текст...');
// createPost('Новость', 'Текст...'); // ОШИБКА: Именованные параметры должны быть указаны явно

```

> **Сравнение :**
>
> - **Позиционные** (Positional): Порядок важен.
> - **Именованные** (Named): Порядок не важен, читаемость высокая, по умолчанию необязательны.
> - **`required` Named:** Лучший подход для создания функций с ясным API.

### 3. Функции Как Объекты Первого Класса (First-Class Citizens)

В Dart функции являются объектами, которые могут быть присвоены переменным, переданы как аргументы и возвращены из других функций. Это основа **функционального программирования**.

#### А. Присвоение Функции Переменной

Вы можете присвоить функцию переменной, используя ее сигнатуру.

```dart
int multiply(int x, int y) => x * y;

// Переменная 'mathOperation' теперь содержит функцию 'multiply'
var mathOperation = multiply;

int result = mathOperation(5, 4); // result = 20

```

#### Б. Функции Высшего Порядка (Higher-Order Functions)

Это функции, которые принимают другие функции в качестве аргументов или возвращают функцию.

```dart
// 1. Функция, которая принимает другую функцию (callback)
void performCalculation(int a, int b, Function operation) {
  print('Результат: ${operation(a, b)}');
}

// 2. Вызов
int subtract(int x, int y) => x - y;
performCalculation(10, 5, subtract); // Вывод: 5

// Тип Function можно заменить на более строгий typedef (см. ниже).

```

### 4. Анонимные Функции (Лямбды)

**Анонимная функция** (или **лямбда-выражение**) — это функция без имени. Они используются повсеместно при работе с коллекциями и асинхронным кодом.

#### Синтаксис:

```dart
(параметры) {
  // Тело функции
};

```

Или, для одного выражения:

```dart
(параметры) => выражение;

```

#### Пример: Использование с List

```dart
List<int> numbers = [1, 2, 3, 4, 5];

// Используем анонимную функцию для итерации (forEach)
numbers.forEach((element) {
  print('Число: $element');
});

// Используем анонимную функцию для преобразования (map)
List<int> doubledNumbers = numbers.map((n) => n * 2).toList();
// doubledNumbers теперь: [2, 4, 6, 8, 10]

```

### 5. Type Aliases (Typedef)

**Typedef** позволяет создать псевдоним (alias) для сигнатуры функции, что улучшает читаемость и типизацию функций высшего порядка.

```dart
// Создаем псевдоним для функции, которая принимает два int и возвращает int
typedef MathOperation = int Function(int a, int b);

// Функция теперь принимает строго типизированный псевдоним
void executeMath(int x, int y, MathOperation op) {
  print('Result: ${op(x, y)}');
}

int divide(int a, int b) => a ~/ b; // Целочисленное деление

executeMath(20, 4, divide); // OK
// executeMath(20, 4, greet); // ОШИБКА КОМПИЛЯЦИИ: greet не соответствует сигнатуре MathOperation

```

> **Совет для собеседования:** Использование `typedef` демонстрирует глубокое понимание системы типов Dart и способность писать чистый, поддерживаемый код.

---

## 🚀 Ваше Задание на Урок 3

1. **Создайте функцию** с названием `createUserProfile`, которая принимает:

- **Обязательный именованный** параметр `required String name`.
- **Обязательный именованный** параметр `required int age`.
- **Необязательный именованный** параметр `String? email` со значением по умолчанию `'default@example.com'`, если он не указан.

2. **Вызовите** эту функцию двумя способами:

- Указав только `name` и `age`.
- Указав все три параметра.

3. **Создайте функцию высшего порядка** `applyFilter`, которая принимает `List<int>` и другую функцию (`int Function(int)`). Эта функция должна возвращать новый список, в котором каждый элемент исходного списка обработан переданной функцией.
4. **Протестируйте** `applyFilter`, передав ей анонимную функцию, которая возводит число в квадрат.

Мы рассмотрели функции как профессионалы. Далее нас ждет **Урок 4: Классы, Объекты и Принципы ООП**, где мы перейдем к объектно-ориентированному стилю Dart!
