Прекрасно! Переходим к **Уроку 5**. В этом уроке мы углубимся в продвинутые концепции Объектно-Ориентированного Программирования (ООП): **наследование, полиморфизм, абстракцию и интерфейсы**. Это фундаментальные знания, необходимые для построения больших и гибких приложений на Dart/Flutter.

## 📚 Урок 5: Наследование, Полиморфизм, Абстракция и Интерфейсы

### 1. Наследование (Inheritance)

**Наследование** позволяет одному классу (дочернему, или **подклассу**) получать свойства и методы другого класса (родительского, или **суперкласса**). В Dart для этого используется ключевое слово **`extends`**.

#### А. Принципы Наследования

```dart
// Суперкласс (Родительский класс)
class Animal {
  String name;

  Animal(this.name);

  void eat() {
    print('$name ест.');
  }
}

// Подкласс (Дочерний класс), наследующий от Animal
class Dog extends Animal {
  // Dog наследует поле 'name' и метод 'eat'

  // Дополнительное поле
  String breed;

  // Конструктор подкласса должен вызывать конструктор суперкласса
  Dog(String name, this.breed) : super(name);

  // Дополнительный метод
  void bark() {
    print('$name лает.');
  }
}

void main() {
  var myDog = Dog('Рекс', 'Овчарка');
  myDog.eat();  // Метод, унаследованный от Animal
  myDog.bark(); // Собственный метод
}

```

**Ключевое слово `super`:** Используется в конструкторе дочернего класса для вызова конструктора родительского класса, чтобы инициализировать унаследованные поля.

#### Б. Переопределение Методов (Method Overriding)

**Переопределение** — это процесс изменения реализации унаследованного метода в дочернем классе. Для этого используется аннотация **`@override`**.

```dart
class Cat extends Animal {
  Cat(String name) : super(name);

  @override // Хорошая практика: явно указываем, что метод переопределяется
  void eat() {
    super.eat(); // Вызываем оригинальный метод родителя (необязательно)
    print('$name пьет молоко.'); // Добавляем собственную логику
  }
}

```

### 2. Полиморфизм (Polymorphism)

**Полиморфизм** (многообразие форм) — это способность объекта принимать несколько форм. В контексте ООП это означает, что объект дочернего класса может быть обработан как объект родительского класса.

```dart
Animal pet1 = Dog('Шарик', 'Дворняга'); // Объект Dog, но переменная типа Animal
Animal pet2 = Cat('Мурка');  // Объект Cat, но переменная типа Animal

List<Animal> zoo = [pet1, pet2];

for (final animal in zoo) {
  // Вызывается правильная (переопределенная) реализация метода eat()
  // в зависимости от фактического типа объекта (Dog или Cat)
  animal.eat();
}
// Вывод:
// Шарик ест.
// Шарик пьет молоко. // Для Cat
// Мурка ест.
// Мурка пьет молоко. // Для Dog

```

### 3. Абстракция (Abstract Classes)

**Абстрактный класс** — это класс, который не может быть создан напрямую (вы не можете создать его экземпляр). Он используется в качестве базового шаблона для других классов.

- Объявляется с ключевым словом **`abstract`**.
- Может содержать **абстрактные методы** (методы без реализации, только с сигнатурой), которые **обязаны** быть реализованы любым неабстрактным дочерним классом.

```dart
abstract class Shape {
  // Абстрактный метод: дочерние классы ДОЛЖНЫ его реализовать
  double calculateArea();

  // Обычный метод (с реализацией)
  void printInfo() {
    print('Это геометрическая фигура.');
  }
}

class Circle extends Shape {
  double radius;

  Circle(this.radius);

  @override
  double calculateArea() {
    return 3.14159 * radius * radius;
  }
}

// var shape = Shape(); // ОШИБКА: Нельзя создать экземпляр абстрактного класса
var circle = Circle(5.0);
print(circle.calculateArea());

```

> **Совет для собеседования:** Абстрактные классы используются, когда вы хотите определить общий "контракт" (обязательные методы) и при этом предоставить базовую реализацию для некоторых общих методов.

### 4. Интерфейсы (Interfaces)

В Dart нет отдельного ключевого слова `interface`. **Любой класс может выступать в качестве интерфейса.**

Когда класс **реализует** (использует ключевое слово **`implements`**) другой класс:

- Он **обязан** реализовать **все** методы и свойства, определенные в реализуемом классе (интерфейсе).
- Он не наследует никакую реализацию, только "контракт" (сигнатуры методов).

```dart
// Класс-интерфейс (любой класс может быть интерфейсом)
class DatabaseConnector {
  void connect();
  void disconnect();
  void executeQuery(String query);
}

// Класс, реализующий (имплементирующий) интерфейс
class PostgreSQLConnector implements DatabaseConnector {
  @override
  void connect() {
    print('Подключение к PostgreSQL...');
  }

  @override
  void disconnect() {
    print('Отключение от PostgreSQL.');
  }

  @override
  void executeQuery(String query) {
    print('Выполнение запроса: $query');
  }
}

void main() {
  DatabaseConnector db = PostgreSQLConnector(); // Полиморфизм через интерфейс
  db.connect();
}

```

> **Когда использовать `extends` (наследование) vs `implements` (интерфейс)?**
>
> - **Наследование (`extends`):** Используйте, когда существует отношение "**является**" (`A` является `B`). Например, `Dog` **является** `Animal`. Вы хотите переиспользовать код (реализацию).
> - **Интерфейс (`implements`):** Используйте, когда существует отношение "**может**" (`A` может `B`). Например, `PostgreSQLConnector` **может** `DatabaseConnector`. Вы хотите определить контракт (обязательные методы) и не переиспользовать реализацию.

### 5. Миксины (Mixins)

**Миксины** — это мощная особенность Dart, позволяющая многократно использовать код класса в других классах **без использования наследования**.

- Объявляются с ключевым словом **`mixin`**.
- Применяются к классу с ключевым словом **`with`**.

```dart
// Миксин для добавления функционала логирования
mixin Logger {
  void log(String message) {
    var timestamp = DateTime.now().toLocal();
    print('[$timestamp] LOG: $message');
  }
}

// Класс, который использует функционал миксина
class PaymentProcessor with Logger {
  void processPayment(double amount) {
    // Используем метод из миксина 'Logger'
    log('Обработка платежа на сумму: $amount');
    // ...
  }
}

void main() {
  var processor = PaymentProcessor();
  processor.processPayment(150.0);
}

```

> **Под Капотом (Как работает `with`):** Когда вы используете `with`, Dart фактически копирует код (методы и поля) миксина в класс, который его использует, на этапе компиляции. Это более гибкий и безопасный способ добиться многократного использования кода, чем множественное наследование (которое запрещено в Dart).

---

## 🚀 Ваше Задание на Урок 5

1. **Создайте абстрактный класс** `Vehicle` (Транспортное средство) с двумя абстрактными методами: `startEngine()` и `stopEngine()`.
2. **Создайте миксин** `FuelEfficient` с одним методом `checkFuelEconomy()`, который выводит строку о расходе топлива.
3. **Создайте класс** `Car`, который:

- **Наследует** от `Vehicle` (`extends`).
- **Использует** миксин `FuelEfficient` (`with`).

4. **Создайте класс** `Bicycle`, который:

- **Реализует** `Vehicle` (`implements`).
- _Примечание_: Поскольку `Vehicle` абстрактен, `Bicycle` должен реализовать `startEngine()` и `stopEngine()`, хотя для велосипеда это может быть бессмысленно (например, выводя `'Не применимо'`).

5. **Продемонстрируйте Полиморфизм:** Создайте список `List<Vehicle>` и добавьте в него объекты `Car` и `Bicycle`. Пройдите по списку и вызовите `startEngine()` для каждого элемента.

Мы освоили основные принципы построения сложной структуры кода! Далее нас ждут **Коллекции и Generics (Обобщения)** (Урок 6), что критически важно для работы с данными.
