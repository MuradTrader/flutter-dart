Отлично\! Вы переходите к специальным операторам управления потоком, которые позволяют вам более тонко контролировать выполнение цикла — **`break`** и **`continue`**.

Автор начинает с объяснения оператора **`continue`**.

---

## 🛑 Оператор `continue` (Продолжить)

Оператор **`continue`** используется внутри цикла (`for`, `while` или `do-while`) для **пропуска текущей итерации** и немедленного перехода к следующей.

### 1\. Как Работает `continue`

Когда Dart встречает ключевое слово `continue`, он:

1.  **Немедленно останавливает** выполнение оставшегося кода в текущем блоке цикла.
2.  **Переходит** к следующему шагу цикла:
    - В цикле `for`: Переходит к части **Изменение** (`i++`) и затем к следующей итерации.
    - В цикле `while`/`do-while`: Переходит к проверке **Условия**.

### 2\. Разбор Примера Автора

Автор использует цикл `for` для перебора строки `"Hello"` и хочет **пропустить** символы с индексами 1, 2 и 3 (то есть буквы 'e', 'l', 'l').

**Исходный Код (в цикле `for`):**

```dart
String value = "Hello"; // H(0), e(1), l(2), l(3), o(4)

for (int i = 0; i < value.length; i++) {

  if (i == 1 || i == 2 || i == 3) {
    continue; // Если I равно 1, 2 или 3, пропустить все ниже
  }

  print(value[i]); // Этот код будет пропущен, если сработает continue
}
```

### 3\. Пошаговое Выполнение

| Итерация | `i` | Условие `if` (`i == 1 |                        |             i == 2             |     | i == 3`) | Действие `continue` | `print(value[i])` | Вывод |
| :------: | :-: | :-------------------: | :--------------------: | :----------------------------: | :-: | -------- | ------------------- | ----------------- | ----- |
|    1     |  0  |         False         |          Нет           | Выполняется (`value[0]` = 'H') |  H  |
|    2     |  1  |         True          | Вызывается `continue`. |          ❌ Пропущено          |  -  |
|    3     |  2  |         True          | Вызывается `continue`. |          ❌ Пропущено          |  -  |
|    4     |  3  |         True          | Вызывается `continue`. |          ❌ Пропущено          |  -  |
|    5     |  4  |         False         |          Нет           | Выполняется (`value[4]` = 'o') |  o  |

**Итог:** Напечатаны только символы 'H' и 'o'.

### 4\. Роль `continue` в Цикле `for`

Автор подчеркивает, что в цикле `for` оператор `continue` автоматически переходит к части **Изменение** (`i++`), прежде чем проверить условие для следующего цикла.

> **Сравнение:** Если бы вы использовали **`continue`** в цикле **`while`**, вы должны были бы убедиться, что **оператор изменения** (`i++`) находится **до** оператора `continue`, иначе вы могли бы легко создать бесконечный цикл (поскольку `i` никогда не увеличится, а условие всегда будет истинно). В цикле `for` эта логика уже встроена в его структуру.

В следующем видео вы, вероятно, рассмотрите оператор **`break`**, который полностью завершает цикл.
