Отлично\! Теперь, когда вы освоили переменные, автор переходит к **Управлению Потоком** (Control Flow) — механизму, который позволяет программе принимать решения и выполнять разные действия в зависимости от условий.

---

## 🚦 Управление Потоком (Control Flow)

Управление потоком определяет порядок, в котором выполняются инструкции в программе. Ключевым инструментом для принятия решений является **условный оператор `if`**.

### 1\. Оператор `if`

Оператор `if` (если) позволяет выполнить блок кода, только если определенное **условие** является истинным (`true`).

**Базовый Синтаксис:**

```dart
if (условие) {
  // Код, который выполняется, если условие истинно (true)
}
```

**Пример:** Проверка совершеннолетия.

```dart
int age = 18;

if (age >= 18) {
  print("Взрослый (Adult)");
}
```

### 2\. Условие `else` (Иначе)

Оператор `else` предоставляет альтернативный блок кода, который выполняется, если условие `if` является ложным (`false`).

**Синтаксис `if-else`:**

```dart
if (age >= 18) {
  print("Взрослый");
} else {
  print("Ребенок (Child)"); // Выполняется, если age < 18
}
```

**Ключевой момент:** Блок `else` **не имеет условия** и выполняется, только если **все предыдущие** условия (`if` или `else if`) оказались ложными. Блок `else` является **необязательным** (_optional_).

### 3\. Условие `else if` (Иначе, если...)

Оператор `else if` позволяет проверить **дополнительные** условия, если предыдущее условие `if` (или `else if`) было ложным.

**Синтаксис `if-else if-else` (Каскад условий):**

```dart
int age = 21;

if (age >= 21) {
  print("Взрослый 21+"); // ✅ Это условие истинно
} else if (age >= 18) {
  print("Взрослый 18+"); // ❌ Это условие НЕ ПРОВЕРЯЕТСЯ
} else {
  print("Ребенок");
}
```

**Важное Правило Каскада:**
Когда в структуре `if-else if-else` **одно** условие оказывается истинным (`true`), соответствующий блок кода выполняется, и **все остальные** блоки (`else if` и `else`) **игнорируются** (не проверяются и не выполняются).

> **Пример автора:** Если `age = 21`, и первое условие (`age >= 21`) истинно, программа сразу же выведет "Взрослый 21+" и перейдет к коду после всего блока `if-else-if`, игнорируя второе условие (`age >= 18`).

Для корректной работы важно располагать условия от **самого строгого/конкретного** к **наименее строгому/общему** (например, сначала `>= 21`, потом `>= 18`).

---

## 🎯 Реляционные и Логические Операторы

Условие внутри скобок `if()` должно быть **булевым выражением** (_Boolean expression_), то есть его результатом может быть только **`true`** (истина) или **`false`** (ложь).

### 1\. Реляционные Операторы (Relational Operators)

Эти операторы используются для **сравнения** двух значений и возвращают булево значение.

| Оператор | Название         | Значение (Математика)                    |
| :------- | :--------------- | :--------------------------------------- |
| **`==`** | Равно            | Проверяет, **равны ли** два значения.    |
| **`!=`** | Не равно         | Проверяет, **не равны ли** два значения. |
| **`>`**  | Больше чем       | Проверяет, больше ли левый операнд.      |
| **`<`**  | Меньше чем       | Проверяет, меньше ли левый операнд.      |
| **`>=`** | Больше или равно | Проверяет, больше или равно ли.          |
| **`<=`** | Меньше или равно | Проверяет, меньше или равно ли.          |

> **Критическое различие:**
>
> - **`=`** (один знак равно) — это **Оператор присваивания** (_Assignment Operator_). Он **присваивает** значение переменной.
> - **`==`** (два знака равно) — это **Оператор отношения** (_Relational Operator_). Он **сравнивает** два значения.

**Ошибка:** В условии `if` нельзя использовать оператор присваивания: `if (age = 18)` ❌. Это вызовет ошибку, поскольку условие должно быть булевым, а присваивание возвращает `int`, а не `bool`.

### 2\. Логические Операторы (Logical Operators)

Эти операторы используются для объединения нескольких булевых выражений в одно.

| Оператор | Название       | Условие истинности                                                      |
| :------- | :------------- | :---------------------------------------------------------------------- | -------------- | -------------------------------------------------------------- |
| **`&&`** | **И** (_AND_)  | Оба условия (слева и справа) должны быть **истинными** (`true`).        |
| \*\*`    |                | `\*\*                                                                   | **ИЛИ** (_OR_) | **Хотя бы одно** из условий должно быть **истинным** (`true`). |
| **`!`**  | **НЕ** (_NOT_) | Инвертирует (обращает) булево значение (`!true` $\rightarrow$ `false`). |

**Примеры:**

- **`&&` (И):**
  ```dart
  if (age >= 18 && isAllowed == true) { // Обе части должны быть true
      print("Разрешен вход");
  }
  ```
- **`||` (ИЛИ):**
  ```dart
  if (age < 18 || isAllowed == false) { // Достаточно, чтобы одна из частей была true
      print("Вход запрещен");
  }
  ```
- **`!` (НЕ):**
  ```dart
  if (!isAllowed) { // То же самое, что и if (isAllowed == false)
      print("Не разрешено");
  }
  ```

### 3\. Сравнение Разных Типов

Dart не позволяет сравнивать несопоставимые типы (если они не являются обнуляемыми или динамическими).

- `if (someString == 18)` ❌. Нельзя сравнивать строку (`String`) с целым числом (`int`).
- Сравнение возможно только для **совместимых** типов, например, числа с числами, строки со строками.

---

## 📦 Область Видимости (Scope)

Автор объясняет, почему после `if` и `else` используются **фигурные скобки** (`{}`).

Фигурные скобки определяют **область видимости** (_scope_) — блок кода, внутри которого существуют объявленные переменные.

- **Локальная переменная:** Переменная, объявленная **внутри** блока (`{...}`), существует только в пределах этого блока.

- **Пример:** Если вы объявляете `String someValue = "здесь";` внутри блока `if`, вы **не сможете** получить к ней доступ за пределами этого блока `if` (например, в блоке `else` или в коде, который идет после всего `if-else`):

  ```dart
  if (true) {
    String insideIf = "Hello"; // Видна только внутри этих скобок
  }
  // print(insideIf); // ❌ Ошибка: Undefined name 'insideIf'
  ```

- **Вывод:** Чтобы переменная была доступна для всего блока `if-else-if`, она должна быть объявлена **вне** его (в родительской области видимости, например, в функции `main()`).

---

## 🔄 Вложенные Условия (Nested Ifs)

Условия `if` могут быть **вложенными** (_nested_), то есть один оператор `if` находится внутри другого. Это позволяет проверять более сложные и последовательные логические цепочки.

```dart
if (age >= 18) { // Первое условие
  if (!isAllowed) { // Вложенное условие (проверяется, только если первое истинно)
    print("Взрослый, но запрещено");
  } else {
    print("Взрослый и разрешено");
  }
} else {
  print("Ребенок");
}
```

---

## ⚡ Тернарный Оператор (Ternary Operator)

Хотя автор только упоминает, что он будет рассмотрен, тернарный оператор тесно связан с условными операторами и является **краткой формой** записи простого `if-else`.

Он позволяет **одним выражением** выбрать одно из двух значений в зависимости от условия.

**Синтаксис:**

$$\text{условие} \space ? \space \text{значение\_если\_true} \space : \space \text{значение\_если\_false};$$

(Это будет рассмотрено подробнее в следующем разделе курса).
