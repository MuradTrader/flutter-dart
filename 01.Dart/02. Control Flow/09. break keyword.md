Здравствуйте\! Автор переходит ко второму оператору управления циклом — **`break`**. Этот оператор имеет более драматичный эффект, чем `continue`, поскольку он **полностью останавливает** цикл.

---

## 💥 Оператор `break` (Прервать)

Оператор **`break`** используется для немедленного и безвозвратного **выхода** из ближайшего объемлющего цикла (`for`, `while`, `do-while`) или блока `switch`.

### 1\. Как Работает `break`

Когда Dart встречает ключевое слово `break`:

1.  Он **немедленно завершает** выполнение текущего цикла (или блока `switch`).
2.  Выполнение программы **переходит** к первой инструкции, находящейся **сразу после** цикла/блока `switch`.

### 2\. Разбор Примера Автора

Автор использует тот же цикл `for` для перебора строки `"Hello"`, но заменяет `continue` на `break`.

**Исходный Код (в цикле `for`):**

```dart
String value = "Hello"; // H(0), e(1), l(2), l(3), o(4)

for (int i = 0; i < value.length; i++) {

  if (i == 1 || i == 2 || i == 3) {
    break; // Если I равно 1, 2 или 3, немедленно выйти из цикла!
  }

  print(value[i]);
}
```

### 3\. Пошаговое Выполнение

| Итерация  | `i` | Условие `if` |             Действие              | Вывод |
| :-------: | :-: | :----------: | :-------------------------------: | :---: |
|     1     |  0  |    False     |         `print(value[0])`         |   H   |
|     2     |  1  |     True     |      Вызывается **`break`**.      |   -   |
| **КОНЕЦ** |  -  |      -       | **Цикл немедленно прекращается.** |   -   |

**Итог:** Напечатана только 'H'. Как только `i` достигает 1, условие истинно, и `break` **выбрасывает** выполнение программы за пределы фигурных скобок цикла `for`.

### 4\. `break` против `continue`

Это самое важное различие, которое необходимо усвоить:

| Оператор       | Назначение     | Эффект                                                                                   |
| :------------- | :------------- | :--------------------------------------------------------------------------------------- |
| **`continue`** | **Пропустить** | Пропускает **оставшуюся часть текущей итерации** и переходит к следующей итерации цикла. |
| **`break`**    | **Прервать**   | **Полностью завершает** цикл и переходит к коду, идущему после цикла.                    |

### 5\. Область Действия `break`

Автор подчеркивает распространенное заблуждение:

> **Важно:** `break` выводит вас **только из того цикла или блока `switch`**, в котором он был использован. Он **не завершает** функцию (например, функцию `main`), если только сам цикл не был последней инструкцией в этой функции.

### 6\. Вспоминаем `switch`

Автор напоминает, что вы уже сталкивались с `break` при изучении оператора `switch`:

- **До Dart 3.0:** В большинстве языков (`Java`, `C++`) `break` был **обязателен** после каждого непустого блока `case`, чтобы предотвратить **провал** (_fall-through_) в следующий `case`.
- **В Dart (начиная с 3.0):** `break` **не требуется** в непустых блоках `case`, потому что Dart предотвращает `fall-through` автоматически. Однако он по-прежнему необходим в **пустых** блоках `case` для явного указания, что нет провала в следующий блок.
