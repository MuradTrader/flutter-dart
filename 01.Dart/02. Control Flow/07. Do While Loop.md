Здравствуйте\! Автор представляет третий и последний основной тип цикла — **цикл `do-while`**. Этот цикл имеет ключевое отличие от `for` и `while`, связанное с моментом проверки условия.

---

## 🔁 Цикл `do-while` (Выполнить-Пока)

Цикл `do-while` гарантирует, что блок кода будет выполнен **хотя бы один раз**, независимо от того, истинно условие или ложно.

### 1\. Синтаксис `do-while`

В цикле `do-while` условие проверки находится в конце.

$$\mathbf{do} \space \{ \space \text{Блок кода} \space \} \space \mathbf{while} \space (\text{Условие});$$

Обратите внимание, что после условия `while()` **обязательно** ставится точка с запятой (`;`).

### 2\. Ключевое Отличие: Цикл с Постпроверкой (Exit-Controlled Loop)

Автор использует аналогию с охранником для объяснения разницы:

| Тип цикла               | Механизм проверки                                               | Гарантия выполнения                         | Название                                                       |
| :---------------------- | :-------------------------------------------------------------- | :------------------------------------------ | :------------------------------------------------------------- |
| **`for`** и **`while`** | Проверка условия происходит **до** входа в блок.                | Выполнится **только** если условие истинно. | **Цикл с Предварительной Проверкой** (_Entry-Controlled Loop_) |
| **`do-while`**          | Проверка условия происходит **после** первого выполнения блока. | Выполнится **минимум один раз**.            | **Цикл с Постпроверкой** (_Exit-Controlled Loop_)              |

### 3\. Пошаговая Работа `do-while`

Давайте посмотрим на пример автора, где условие **заведомо ложно** (или никогда не будет истинно):

```dart
int i = 0;
// Условие всегда ложно (0 никогда не равно 0)
while (i != i) {
  // Этот блок никогда не выполнится в цикле while или for
}
```

А теперь то же самое с `do-while`:

```dart
int i = 0;
do {
  print(value[i]); // 1. Код выполняется
  i++;             // 2. Счетчик увеличивается (i = 1)
} while (i != i);  // 3. Проверка условия
```

**Разбор выполнения:**

1.  Программа достигает `do` и **сразу** выполняет блок кода. Выводится `value[0]` (буква 'H').
2.  Значение `i` увеличивается до 1.
3.  Программа доходит до `while` и проверяет условие: `i != i`? (т.е. `1 != 1`?). Это **ложно** (`false`).
4.  Цикл **завершается**.

**Результат:** Была напечатана 'H' **один раз**, несмотря на то, что условие изначально и на первой проверке было ложным.

### 4\. Структура `do-while`

Как и в цикле `while`, в `do-while` необходимо вручную заботиться о всех трех частях:

1.  **Инициализация:** Должна быть выполнена **перед** блоком `do`.
2.  **Условие:** Размещается в конце в скобках `while()`.
3.  **Изменение (Инкремент/Декремент):** **Обязательно** должно быть размещено **внутри** блока `do`.

> ⚠️ **Предупреждение:** Автор снова напоминает, что если забыть об операторе изменения (`i++`), цикл `do-while` также станет **бесконечным** после первого выполнения, так как условие будет проверяться, но переменная, от которой оно зависит, не будет меняться.

**Заключение:** `do-while` используется редко, но незаменим, когда вам **нужно гарантировать**, что код (например, запрос данных у пользователя) будет выполнен хотя бы один раз, прежде чем вы сможете проверить, нужно ли повторять его.
