Отлично\! Автор показывает вам второй основной тип цикла — **цикл `while`** (пока), а также демонстрирует, как структура `for` переносится в структуру `while`.

---

## 🔁 Цикл `while` (Пока)

Цикл **`while`** — это более простой, но не менее мощный тип цикла, который выполняет блок кода **до тех пор, пока** заданное булево условие остается истинным.

### 1\. Синтаксис `while`

В отличие от цикла `for`, который объединяет инициализацию, условие и изменение в одной строке, цикл `while` содержит только **условие** в круглых скобках:

$$\mathbf{while} \space (\text{Условие}) \space \{ \space \text{Блок кода} \space \}$$

[Image of while loop syntax diagram showing the condition check and the loop body execution]

- **1. $\mathbf{while}$**: Ключевое слово.
- **2. $\text{Условие}$**: Булево выражение. Если оно `true`, блок кода выполняется. Если оно `false`, цикл завершается.
- **3. $\text{Блок кода}$**: Инструкции, которые будут повторяться.

### 2\. Перенос Логики `for` в `while`

Чтобы цикл `while` работал корректно (и не стал бесконечным\!), ему, как и `for`, необходимы три элемента управления. Однако эти части размещаются в коде по-другому:

| Элемент Управления   | Цикл `for`                 | Цикл `while`                                          |
| :------------------- | :------------------------- | :---------------------------------------------------- |
| **1. Инициализация** | Внутри скобок, в начале.   | **Вне цикла, перед ним.**                             |
| **2. Условие**       | Внутри скобок, в середине. | **Внутри скобок `while()`**.                          |
| **3. Изменение**     | Внутри скобок, в конце.    | **Внутри блока кода `while`** (обычно в самом конце). |

**Пример (аналог `for`):**

```dart
// 1. Инициализация (вне цикла)
int i = 0;
String value = "Hello";

// 2. Условие (в скобках)
while (i < value.length) {

  // Выполняемый код
  print(value[i]);

  // 3. Изменение (внутри блока, чтобы предотвратить бесконечный цикл)
  i++;
}
```

### 3\. Опасность Бесконечного Цикла в `while`

Автор наглядно демонстрирует, как легко создать бесконечный цикл в `while`, если забыть о части **Изменение**:

```dart
int i = 0;
while (i < value.length) {
  print(value[i]);
  // ❌ Здесь нет i++
}
// i всегда остается равным 0, условие 0 < 5 всегда истинно. -> Бесконечный цикл.
```

**Правило:** В цикле `while` **обязательно** убедитесь, что переменная, используемая в условии, **изменяется** внутри блока таким образом, чтобы рано или поздно условие стало ложным (`false`).

### 4\. Область Видимости (`scope`) Переменной `i`

Автор объясняет важное различие в области видимости переменной-счетчика:

- **В цикле `for`:** Переменная, объявленная в Инициализации (`for (int i = 0; ...)`), видна только **внутри** этого цикла.
- **В цикле `while`:** Переменная, объявленная для Инициализации (`int i = 0;`) должна быть создана **вне** цикла `while`. Следовательно, эта переменная **доступна** для использования во всей внешней функции (например, во всей функции `main()`) даже после завершения цикла.

**Вывод:** Если вам нужно, чтобы переменная-счетчик была доступна после завершения цикла, цикл **`while`** (или объявление переменной до цикла `for`) является лучшим выбором.
