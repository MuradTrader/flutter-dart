Отличный практический пример\! Автор курса предоставил вам реальную задачу для закрепления материала по **условным операторам** (`if-else`) и **оператору `switch`**.

Вот подробное объяснение решения и техник, которые использует автор:

---

## 💻 Упражнение: Расчет Стоимости Доставки

**Задача:** Рассчитать стоимость доставки в зависимости от **зоны назначения** (`destinationZone`) и **веса** (`weightInKilograms`), используя заданные тарифы.

### 1\. Объявление Исходных Данных (Variables)

Автор начинает с создания переменных, имитирующих ввод пользователя:

- **`String destinationZone = 'ABC';`** (или `'PQR'`, `'XYZ'`). Тип `String` подходит для текстового названия зоны.
- **`double weightInKilograms = 4.5;`** (или `6.0`). Тип **`double`** (число с плавающей запятой) выбран, потому что вес **может быть дробным**, что является хорошей практикой.

---

## 2\. Решение 1: Использование `if-else if-else`

Автор показывает, как решить задачу, используя каскад условных операторов.

**Логика:** Проверяем зоны последовательно.

```dart
if (destinationZone == 'PQR') {
  // Стоимость: $10 за кг
  // ... расчет стоимости
} else if (destinationZone == 'XYZ') {
  // Стоимость: $5 за кг
  // ... расчет стоимости
} else if (destinationZone == 'ABC') {
  // Стоимость: $7 за кг
  // ... расчет стоимости
} else {
  // Зона не найдена
  print('Invalid destination zone.');
}
```

**Обработка данных (Первый подход автора):**

Изначально автор встраивает расчет и вывод прямо в каждый блок:

```dart
if (destinationZone == 'PQR') {
  print('Shipping cost: ${weightInKilograms * 10}');
} // ... и так далее для других зон.
```

Этот подход работает, но автор позже показывает, как его улучшить.

---

## 3\. Решение 2: Использование Оператора `switch`

Поскольку мы проверяем **одну переменную** (`destinationZone`) на **точное равенство** нескольким фиксированным значениям, `switch` — отличная альтернатива.

**Логика `switch`:**

```dart
switch (destinationZone) {
  case 'PQR':
    // ... расчет $10 за кг
    break; // Не требуется, но можно оставить
  case 'XYZ':
    // ... расчет $5 за кг
    break;
  case 'ABC':
    // ... расчет $7 за кг
    break;
  default:
    print('Invalid destination zone.');
}
```

---

## 4\. Улучшение Кода и Лучшие Практики

Автор демонстрирует два важных улучшения, которые делают код более чистым и управляемым:

### A. Централизация Расчета Стоимости

**Проблема:** Изначально фраза `"Shipping cost:"` повторялась в каждом блоке `if`. Если нужно изменить ее на `"Billing cost:"`, приходится менять код в трех местах.

**Решение:** Объявить переменную для хранения результата (`double cost`) и выполнять **только расчет** внутри условных блоков. Вывод результата — **один раз** в конце.

1.  **Объявление переменной вне `if`:**
    ```dart
    double cost = 0.0;
    // Или другой подход, который будет рассмотрен позже: String? errorMessage;
    ```
2.  **Расчет в блоках `if`:**
    ```dart
    if (destinationZone == 'PQR') {
      cost = weightInKilograms * 10; // Только присвоение значения
    } else if (destinationZone == 'XYZ') {
      cost = weightInKilograms * 5;
    } // ... и т.д.
    ```
3.  **Единый вывод в конце:**
    ```dart
    // Здесь мы можем использовать любое название, например, "Billing cost"
    print('Billing cost: $cost');
    ```

Это улучшает **сопровождаемость** (_maintainability_): изменения в формате вывода делаются только в **одном месте**.

### B. Использование `return` для Прерывания Потока

**Проблема:** Что произойдет, если введена **неверная зона**?

1.  Выполняется блок `else`: `print('Invalid destination zone.');`
2.  Далее, **независимо** от этого, выполняется общий оператор вывода: `print('Billing cost: $cost');`

Если зона была неверной, мы увидим сообщение об ошибке, а затем `Billing cost: 0.0` (так как `cost` не была переприсвоена и осталась `0.0`), что сбивает с толку.

**Решение: Использование `return`**

Автор вводит ключевое слово **`return`** (вернуть) в блок `else`:

```dart
} else {
  print('Invalid destination zone.');
  return; // 🛑 НОВЫЙ ЭЛЕМЕНТ
}

// ... этот код не будет выполнен, если предыдущий блок вернул управление.
print('Billing cost: $cost');
```

**Что делает `return`:**
В данном контексте (внутри функции `main` или любой другой функции) `return` **немедленно завершает выполнение текущей функции**.

- Если зона **неверна**, код выводит ошибку и **завершает** программу (или функцию `main`), **предотвращая** выполнение следующей строки `print('Billing cost: ...');`.
- Если зона **верна**, программа выполняет расчет, не заходит в `else`, и переходит к единой строке вывода `print('Billing cost: ...');`.

Это демонстрирует важный принцип **раннего выхода** (_early exit_) для обработки ошибок.
