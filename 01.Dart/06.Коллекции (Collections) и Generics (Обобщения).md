Отлично! Мы закончили с основными принципами ООП. Теперь переходим к теме, без которой не обходится ни одно реальное приложение: **работа с данными и коллекциями**.

**Урок 6: Коллекции (Collections) и Generics (Обобщения)**

### 1. Generics (Обобщения)

**Generics** — это мощный механизм в Dart, который позволяет создавать типы (классы, функции, методы), которые работают с данными, не уточняя их конкретный тип до момента использования. Это обеспечивает **безопасность типов** во время компиляции, предотвращая ошибки времени выполнения.

#### А. Зачем Нужны Generics?

Рассмотрим список без Generics (используя базовый тип `Object`):

```dart
// List<Object>
var mixedList = []; // По умолчанию Dart может вывести это как List<dynamic>
mixedList.add('Привет');
mixedList.add(123);

// Если мы попытаемся использовать методы String на числе, будет ошибка времени выполнения:
// String s = mixedList[1]; // Ошибка, так как mixedList[1] это int, а не String

```

С Generics:

```dart
// Теперь List гарантированно содержит только String.
List<String> names = ['Анна', 'Борис'];
// names.add(123); // ОШИБКА КОМПИЛЯЦИИ: Нельзя добавить int в List<String>

```

Обобщения обозначаются угловыми скобками (`<>`), внутри которых указывается **параметр типа** (например, `E` для Element, `K` для Key, `V` для Value, или просто `T` для Type).

### 2. Встроенные Типы Коллекций Dart

Dart предоставляет три основных класса коллекций, которые почти всегда используются с Generics: `List`, `Set` и `Map`.

#### А. List (Списки)

- **Назначение:** Упорядоченная (индексированная) коллекция объектов.
- **Особенности:** Позволяет дубликаты. Доступ к элементам по числовому индексу (начиная с 0).

```dart
// Список целых чисел
List<int> scores = [90, 85, 90, 78];

// Добавление:
scores.add(92);

// Доступ:
print(scores[0]); // 90

// Получение длины:
print(scores.length); // 5

```

#### Б. Set (Множества)

- **Назначение:** Неупорядоченная коллекция **уникальных** объектов.
- **Особенности:** Не позволяет дубликаты. Отлично подходит для проверки наличия элемента.

```dart
// Множество строк
Set<String> uniqueTags = {'dart', 'flutter', 'dart'}; // Дубликаты игнорируются

// Добавление:
uniqueTags.add('oop');
uniqueTags.add('dart'); // Игнорируется

// Проверка наличия (очень быстро):
print(uniqueTags.contains('flutter')); // true

```

#### В. Map (Карты/Словари)

- **Назначение:** Коллекция пар **ключ-значение** (Key-Value Pair).
- **Особенности:** Каждый ключ должен быть **уникальным**. Используется для быстрого поиска значения по ключу.

```dart
// Map с ключами типа String и значениями типа int
Map<String, int> inventory = {
  'Apple': 50,
  'Banana': 20,
};

// Добавление/Обновление:
inventory['Orange'] = 30;
inventory['Apple'] = 55; // Обновление

// Доступ:
print(inventory['Banana']); // 20

// Итерация по ключам:
for (final key in inventory.keys) {
  print('Товар: $key');
}

```

### 3. Расширенные Возможности Коллекций (Collection Capabilities)

Dart предоставляет мощные методы для работы с коллекциями, основанные на функциональном программировании и лямбдах.

#### А. Map, Where, Reduce/Fold

Это основные функциональные операции для обработки списков:

| Метод          | Назначение                                                                                                    | Пример                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| **`map()`**    | **Преобразование:** Создает новую коллекцию, преобразуя каждый элемент исходной коллекции.                    | `[1, 2, 3].map((n) => n * 10).toList()` `[10, 20, 30]`  |
| **`where()`**  | **Фильтрация:** Создает новую коллекцию, содержащую только элементы, удовлетворяющие заданному условию.       | `[1, 2, 3, 4].where((n) => n.isEven).toList()` `[2, 4]` |
| **`reduce()`** | **Агрегация:** Объединяет все элементы коллекции в одно значение.                                             | `[1, 2, 3].reduce((sum, n) => sum + n)` `6`             |
| **`fold()`**   | **Агрегация с начальным значением:** Похож на `reduce`, но позволяет задать начальное значение для агрегации. | `[1, 2, 3].fold(10, (sum, n) => sum + n)` `16`          |

#### Б. Spread Operator (`...`)

Оператор `...` позволяет вставлять все элементы одной коллекции в другую.

```dart
List<int> primary = [1, 2, 3];
List<int> secondary = [4, 5];

List<int> allNumbers = [...primary, 0, ...secondary];
// allNumbers = [1, 2, 3, 0, 4, 5]

```

#### В. Collection If и Collection For

Эти конструкции позволяют условно включать элементы или использовать циклы прямо внутри объявления коллекции (очень полезно для Flutter UI).

```dart
bool showAdmin = true;
String userName = 'Alex';

List<String> navBarItems = [
  'Главная',
  'Профиль',

  // Collection If: включает элемент только если условие истинно
  if (showAdmin) 'Админ Панель',

  // Collection For: добавляет элементы через цикл
  for (int i = 0; i < 2; i++) 'Деталь $i',
];
// navBarItems = ['Главная', 'Профиль', 'Админ Панель', 'Деталь 0', 'Деталь 1']

```

### 4. Создание Собственных Generics

Вы можете сделать свои классы и методы обобщенными, используя параметры типа.

#### А. Обобщенный Класс

Создадим класс-контейнер, который может хранить данные любого типа, гарантируя при этом безопасность типов.

```dart
// Класс Box<T> обобщен по типу T
class Box<T> {
  T value; // Поле типа T

  Box(this.value);

  // Метод, который возвращает T
  T unwrap() => value;
}

void main() {
  // 1. Создание Box для int
  var intBox = Box<int>(100);
  int num = intBox.unwrap(); // num гарантированно int

  // 2. Создание Box для String
  var stringBox = Box<String>('Hello Dart');
  String text = stringBox.unwrap(); // text гарантированно String

  // var error = intBox.unwrap() as String; // Ошибка времени выполнения! Компилятор безопасен.
}

```

#### Б. Ограничение Типов (Bounded Generics)

Иногда нужно, чтобы обобщенный тип был только подтипом определенного класса. Для этого используется ключевое слово **`extends`** в объявлении дженерика.

```dart
// Ограничиваем T так, чтобы это был либо Animal, либо его подкласс (Dog, Cat)
class Veterinarian<T extends Animal> {
  void heal(T animal) {
    print('Ветеринар лечит ${animal.name}.');
  }
}

// Veterinarian<String> vet = Veterinarian<String>(); // ОШИБКА: String не extends Animal

class Horse extends Animal {
  Horse(super.name);
}

Veterinarian<Horse> horseVet = Veterinarian<Horse>(); // OK
horseVet.heal(Horse('Спирит'));

```

> **Под Капотом (Soundness):** Dart Generics являются **звуковыми** (Sound). Это означает, что компилятор Dart (благодаря Generics) может гарантировать, что во время выполнения не возникнет ошибка, связанная с несоответствием типов (например, вы никогда не получите `String` из `List<int>`). Это критически важное преимущество перед не-звуковыми дженериками в некоторых других языках.

---

## 🚀 Ваше Задание на Урок 6

1. **Создайте Map** под названием `products` с ключами `String` (название продукта) и значениями `double` (цена). Используйте явное указание типов: `Map<String, double> products = {...};`. Добавьте несколько элементов.
2. **Используйте функциональные методы:**

- **`where`:** Отфильтруйте `products` в новый `Map`, содержащий только продукты, цена которых превышает 50.0.
- **`map`:** Преобразуйте исходный `products` в новый `List<String>`, содержащий только названия продуктов, начинающиеся с заглавной буквы.

3. **Создайте обобщенный класс** `Result<T, E>`. Этот класс должен быть использован для передачи результата операции:

- `T` — тип данных в случае успеха (например, `User` или `List<int>`).
- `E` — тип ошибки в случае неудачи (например, `String` или `int` код ошибки).
- Класс должен содержать два поля: `T? data` и `E? error`.
- Добавьте конструктор `Result.success(T data)` и `Result.failure(E error)`.

4. Продемонстрируйте использование `Result` для успешной операции с `String` и неуспешной операции с `int` кодом ошибки.

Мы заложили прочную основу для работы с данными. На следующем уроке мы перейдем к **Уроку 7: Асинхронное Программирование (Async, Await, Future)**, что абсолютно необходимо для Flutter и бэкенда.
