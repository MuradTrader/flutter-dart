Превосходно! Это означает, что вы освоили основы структуры, типов, безопасности и функционального программирования. Теперь мы переходим к сердцу Dart — **Объектно-Ориентированному Программированию (ООП)**, поскольку Dart является полностью объектно-ориентированным языком.

## 📚 Урок 4: Классы, Объекты и Основы ООП

### 1. Классы и Объекты

**Класс** — это чертеж (план, шаблон) для создания объектов. Он определяет данные (поля/свойства) и поведение (методы), которые будут иметь объекты этого типа.

**Объект** (или **экземпляр**) — это конкретная реализация класса, созданная на основе этого чертежа.

#### А. Объявление Класса

```dart
// Класс User является чертежом для создания объектов-пользователей
class User {
  // Поля (Свойства/Атрибуты) - хранилища данных
  String name;
  int age;

  // Метод (Поведение) - функция, связанная с объектом
  void greet() {
    print('Привет! Меня зовут $name и мне $age лет.');
  }
}

void main() {
  // 1. Создание Объекта (Экземпляра) Класса
  // Используется ключевое слово 'new' (хотя оно необязательно в современном Dart)
  var user1 = User(); // Вызывается конструктор по умолчанию

  // 2. Присвоение значений полям
  user1.name = 'Алексей';
  user1.age = 28;

  // 3. Вызов метода объекта
  user1.greet(); // Вывод: Привет! Меня зовут Алексей и мне 28 лет.
}

```

### 2. Конструкторы (Constructors)

Конструктор — это специальный метод, который вызывается при создании объекта. Его основная задача — инициализировать поля объекта.

#### А. Конструктор по Умолчанию (Default Constructor)

Если вы не объявляете конструктор, Dart предоставляет неявный конструктор без аргументов.

#### Б. Параметризованный Конструктор (Generative Constructor)

Для профессиональной разработки всегда следует явно определять конструктор для обеспечения правильной инициализации.

**Сокращенный синтаксис (The Syntax Sugar):** Dart позволяет использовать синтаксис `this.fieldName` для автоматического присвоения аргументов полям класса.

```dart
class Product {
  String name;
  final double price; // Поле final должно быть инициализировано в конструкторе

  // Параметризованный конструктор:
  // Аргументы автоматически присваиваются соответствующим полям класса (this.name = name)
  Product(this.name, this.price);

  // Метод для форматированного вывода
  String get details => '$name стоит \$${price.toStringAsFixed(2)}';
}

void main() {
  var laptop = Product('Макбук Про', 1599.99);
  print(laptop.details); // Вывод: Макбук Про стоит $1599.99
}

```

#### В. Именованные Конструкторы (Named Constructors)

Класс может иметь несколько конструкторов, если у них разные имена. Это позволяет создавать объекты разными способами (паттерн **Factory**).

```dart
class Point {
  final double x;
  final double y;

  // 1. Основной (позиционный) конструктор
  Point(this.x, this.y);

  // 2. Именованный конструктор для создания точки в начале координат
  Point.origin() : x = 0.0, y = 0.0;

  // 3. Именованный конструктор для создания точки из Map (JSON)
  Point.fromJson(Map<String, double> json)
      : x = json['x']!, // Используем ! (Null Assertion) только если уверены в наличии ключа
        y = json['y']!;
}

void main() {
  var p1 = Point(10.0, 5.0);
  var p2 = Point.origin(); // Вызываем именованный конструктор
  var p3 = Point.fromJson({'x': 1.0, 'y': 2.0});
}

```

### 3. Геттеры и Сеттеры (Getters and Setters)

В ООП желательно скрывать прямое обращение к полям (Инкапсуляция). **Геттеры** и **Сеттеры** позволяют управлять доступом к полям и/или вычислять значения.

- **Геттер (`get`):** Позволяет получить значение, используя синтаксис поля, но внутри может выполнять логику (вычисления).
- **Сеттер (`set`):** Позволяет установить значение, используя синтаксис поля, но внутри может выполнять проверку или преобразование данных.

```dart
class Rectangle {
  double width;
  double height;

  Rectangle(this.width, this.height);

  // Геттер: Вычисляемое свойство (пользователь вызывает как поле: rect.area)
  double get area {
    return width * height;
  }

  // Сеттер: Позволяет установить высоту и ширину одновременно (пользователь вызывает: rect.size = 10;)
  set size(double value) {
    if (value <= 0) {
      throw ArgumentError('Размер должен быть положительным!');
    }
    width = value;
    height = value;
  }
}

```

### 4. Приватность и Инкапсуляция ("Под Капотом")

В отличие от Java или C++, Dart **не использует** ключевые слова `public`, `private`, `protected`.

**Как работает приватность в Dart:**

> Поле, метод или класс считается **приватным** (доступным только внутри _текущего файла_ — **библиотеки**), если его имя начинается с **подчеркивания** (`_`).

Пример:

- **`_internalName`**: Приватное поле.
- **`_calculate()`**: Приватный метод.

```dart
// Файл: user.dart
class User {
  String name;
  int _age; // Приватное поле! Доступно только в user.dart

  User(this.name, this._age);

  // Общедоступный геттер для чтения приватного поля
  int get age => _age;

  // Общедоступный метод для безопасного изменения приватного поля (Инкапсуляция)
  void increaseAge() {
    _age++;
    _logAction('Возраст увеличен');
  }

  // Приватный метод
  void _logAction(String action) {
    print('[$name] Выполнено: $action');
  }
}
// Если этот код находится в файле main.dart, а класс User в user.dart,
// то мы НЕ СМОЖЕМ обратиться к _age напрямую, только через геттер age.

```

> **Профессиональный Совет:** Всегда делайте поля приватными (`_fieldName`) и предоставляйте контролируемый доступ к ним через **публичные** Геттеры/Сеттеры. Это — **Инкапсуляция** в действии.

### 5. Ключевое Слово `static`

Ключевое слово `static` используется для членов класса (полей и методов), которые принадлежат **самому классу**, а не конкретному **объекту** этого класса.

- **Статическое поле:** Доступно через имя класса (`MathUtils.PI`). Все объекты класса используют одну и ту же копию этого поля.
- **Статический метод:** Не имеет доступа к полям конкретного объекта (`this`). Используется для вспомогательных функций, которые логически связаны с классом.

```dart
class MathUtils {
  static const double PI = 3.14159; // Статическая константа
  static int _instanceCount = 0; // Приватное статическое поле для счетчика

  MathUtils() {
    _instanceCount++;
  }

  // Статический метод, не требует создания объекта MathUtils
  static double calculateCircleArea(double radius) {
    return PI * radius * radius;
  }

  // Статический геттер для получения количества созданных объектов
  static int get count => _instanceCount;
}

void main() {
  print(MathUtils.PI); // Доступ через имя класса
  print(MathUtils.calculateCircleArea(5.0));

  MathUtils();
  MathUtils();
  print('Создано объектов: ${MathUtils.count}'); // count = 2
}

```

---

## 🚀 Ваше Задание на Урок 4

1. **Создайте класс** `BankAccount` с двумя приватными полями: `String _accountHolder` (имя владельца) и `double _balance` (баланс), инициализированным в конструкторе.
2. **Реализуйте Инкапсуляцию:**

- Создайте **Геттер** `accountHolder`, который просто возвращает имя владельца.
- Создайте **Геттер** `balance`, который возвращает текущий баланс.
- Создайте **Метод** `deposit(double amount)` для пополнения счета, который проверяет, что `amount > 0`.
- Создайте **Метод** `withdraw(double amount)` для снятия средств, который проверяет, что `amount > 0` и что на счете достаточно средств.

3. **Создайте Статический Метод** в классе `BankAccount` с именем `getBankName()`, который возвращает строку `'Dart National Bank'`.
4. Создайте экземпляр `BankAccount` и протестируйте все методы и геттеры.

После того, как вы уверенно освоите классы и инкапсуляцию, мы перейдем к более сложным темам ООП: **Наследование, Абстрактные Классы и Интерфейсы** (Урок 5).
