Здравствуйте\! Вы переходите к знакомству с **Записями** (_Records_) — новой мощной возможностью Dart 3, которая решает проблему возврата нескольких значений из одной функции.

---

## 📦 Записи (Records) в Dart 3: Возврат Нескольких Значений

Автор объясняет, что в большинстве языков программирования функция может иметь только **один** тип возвращаемого значения. До Dart 3, если вам нужно было вернуть несколько значений, приходилось использовать сложные объекты или карты.

### 1\. Проблема: Ограничение Одним Типом

Невозможно указать несколько типов данных в качестве возвращаемого значения функции напрямую:

```dart
// ❌ Невалидный синтаксис
int, String functionName() {
  // ...
}
```

### 2\. Решение: Синтаксис Записей (Records Syntax)

**Запись** (_Record_, или **Кортеж** — _Tuple_ в других языках) — это анонимный, неизменяемый агрегат фиксированного размера, который используется для объединения нескольких значений.

Чтобы функция возвращала Запись, вы заключаете типы возвращаемых значений в **круглые скобки** в объявлении функции, а сами возвращаемые значения — в скобки в операторе `return`.

**Синтаксис объявления и возврата:**

```dart
// 1. Объявление функции:
// Функция возвращает Запись, состоящую из (int, String, bool, String)
(int, String, bool, String) printName() {

  // 2. Возврат:
  // Возвращаемые значения должны соответствовать объявленным типам
  return (12, "Raven", false, "11");
}
```

**Ключевые моменты:**

- **Порядок имеет значение:** Порядок типов в объявлении функции (`int`, `String`...) должен **точно соответствовать** порядку возвращаемых значений (`12`, `"Raven"`...).
- **Типовая безопасность:** Dart строго следит за тем, чтобы типы внутри Записи соответствовали объявлению. Если попытаться вернуть `String` вместо `int` на первой позиции, возникнет **ошибка компиляции** (_compile-time error_).

---

## 🔑 Доступ к Значениям Записи: Позиционные Аксессоры (Accessors)

Когда вы сохраняете результат Записи в переменную, вам нужен способ получить доступ к отдельным элементам внутри этой Записи.

Для доступа к элементам Записи в Dart используются **позиционные аксессоры** (Positional Accessors): **`$1`**, **`$2`**, **`$3`** и т.д.

**Пример:**

```dart
// Вызов функции и сохранение Записи в переменной
var myRecord = printName();

// Доступ к первому элементу (int)
var firstValue = myRecord.$1; // Получит 12

// Доступ ко второму элементу (String), как в примере автора
var name = myRecord.$2; // Получит "Raven"
```

### Важные детали:

1.  **Позиционный счет:** Счет начинается с **1**, а не с 0 (в отличие от индексов массивов или строк).
2.  **Типовая осведомленность:** Компилятор Dart **знает тип** каждого аксессора. `myRecord.$2` будет иметь тип `String` (как было объявлено), что обеспечивает **типовую безопасность** при работе с извлеченным значением.

> **Геттеры (Getters):** Автор упоминает, что `$1`, `$2` и т.д. называются **геттерами** (_getters_). Геттеры — это специальный вид метода, который позволяет получать (читать) значение свойства, используя синтаксис доступа к свойству (через точку), как если бы это была обычная переменная.

---

## 🛑 Ограничения `const`

Автор напоминает, что даже с использованием Записей, вы **не можете** использовать ключевое слово **`const`** для хранения результата функции, поскольку Запись, как и любое другое значение, возвращаемое функцией, является результатом вычисления **во время выполнения** (_runtime_), а не константой, известной **во время компиляции** (_compile-time_). Для хранения результата функции используйте **`var`** или **`final`**.
