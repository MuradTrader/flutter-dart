Превосходно! Мы освоили ключевые аспекты асинхронности и работы с потоками. Теперь мы переходим к **Уроку 8**, который фокусируется на том, как профессиональные разработчики обрабатывают нештатные ситуации — **исключения и ошибки**, а также на том, как мы используем **метаданные** (аннотации) для улучшения читаемости и взаимодействия с инструментами Dart.

## 📚 Урок 8: Обработка Ошибок, Исключения и Метаданные (Аннотации)

### 1. Обработка Исключений (Exceptions)

В Dart, как и в большинстве языков, исключения используются для обозначения нештатных ситуаций, которые программа не может обработать самостоятельно (например, деление на ноль, неверный формат данных).

#### А. `try-catch-finally`

Это стандартный механизм для перехвата исключений:

```dart
void performDivision(int numerator, int denominator) {
  try {
    // Код, который может выбросить исключение
    if (denominator == 0) {
      throw IntegerDivisionByZeroException(); // Использование встроенного исключения
    }
    double result = numerator / denominator;
    print('Результат: $result');

  } on IntegerDivisionByZeroException {
    // catch с on: Перехват только конкретного типа исключения
    print('Ошибка: Попытка деления на ноль!');

  } on Exception catch (e) {
    // catch с типом (Exception): Перехват всех исключений, наследующихся от Exception
    print('Перехвачено общее исключение: $e');

  } catch (e, s) {
    // Перехват ЛЮБОГО объекта (e) и получение Stack Trace (s)
    print('Неизвестная ошибка: $e');
    // Stack Trace (s) - это детали, где именно произошла ошибка (для отладки)

  } finally {
    // Выполняется всегда, независимо от того, было ли исключение.
    print('Очистка ресурсов завершена.');
  }
}

void main() {
  performDivision(10, 0);
  performDivision(10, 2);
}

```

#### Б. Ключевые слова `throw` и `rethrow`

- **`throw`**: Используется для явного **выбрасывания** (генерации) исключения. Вы можете выбросить любой объект, но хорошей практикой является выбрасывание объектов, наследующихся от `Exception` или `Error`.
- **`rethrow`**: Используется внутри блока `catch`. Позволяет обработать исключение (например, записать в лог) и затем **повторно выбросить** его, чтобы оно было перехвачено внешним `try-catch`.

### 2. Создание Пользовательских Исключений

Профессиональный код использует собственные, семантически значимые исключения для лучшей обработки ошибок.

Правило: наследуйтесь от **`Exception`**.

```dart
// 1. Создаем наше собственное исключение, наследуя от Exception
class AuthenticationException implements Exception {
  final String message;

  AuthenticationException(this.message);

  // Переопределяем toString() для красивого вывода
  @override
  String toString() => 'AuthenticationException: $message';
}

void login(String user, String pass) {
  if (user != 'admin') {
    // 2. Выбрасываем наше исключение
    throw AuthenticationException('Неверный логин пользователя: $user');
  }
  print('Вход успешен.');
}

void main() {
  try {
    login('guest', '123');
  } on AuthenticationException catch (e) {
    // 3. Конкретный перехват
    print('Ошибка аутентификации: ${e.message}');
  }
}

```

> **Под Капотом (Exception vs Error):**
>
> - **`Exception`:** Ошибки, от которых можно и нужно восстанавливаться (например, потеря сети, неверный ввод). Их следует ловить.
> - **`Error`:** Ошибки, которые указывают на серьезные проблемы в самой программе (например, `StackOverflowError`, `OutOfMemoryError`). От них, как правило, не восстанавливаются, и их не следует ловить.

### 3. Метаданные (Аннотации)

**Метаданные** в Dart — это информация, которую вы добавляете в код, чтобы дать указания компилятору, анализатору или инструментам (например, Flutter), не влияя при этом на сам процесс выполнения программы.

Метаданные начинаются со знака **`@`** (собачка) и применяются к библиотекам, классам, конструкторам, методам, параметрам или полям.

#### А. Встроенные Аннотации

Dart SDK предоставляет несколько полезных встроенных аннотаций:

| Аннотация                | Назначение                                                                                                                                                                               |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`@override`**          | Указывает, что метод переопределяет метод суперкласса. **Обязательно используйте** — анализатор проверит, существует ли родительский метод, и выдаст ошибку, если нет.                   |
| **`@deprecated`**        | Помечает метод или класс как устаревший. Анализатор выдаст предупреждение, если вы попытаетесь его использовать.                                                                         |
| **`@required`**          | _Устарела и заменена на `required` в параметрах функции_.                                                                                                                                |
| **`@protected`**         | Используется во Flutter/Foundation. Указывает, что член должен использоваться только внутри класса и его подклассов (хотя не имеет принудительной силы в Dart, как `_` для приватности). |
| **`@visibleForTesting`** | Указывает, что поле или метод сделано публичным _только_ для целей тестирования.                                                                                                         |

#### Б. Создание Пользовательских Аннотаций

Вы можете создавать свои собственные аннотации, которые могут быть полезны для **кодогенерации** (например, для сериализации JSON) или для статического анализа.

Чтобы создать аннотацию, просто объявите класс, который содержит конструктор `const`.

```dart
// Аннотация для пометки методов, которые должны быть протестированы.
class MustTest {
  final String reason;

  // Конструктор должен быть const!
  const MustTest(this.reason);
}

class UserService {
  @MustTest('Логика расчета очень сложная') // Применяем аннотацию
  int calculateScore(int value) {
    // ...
    return value * 10;
  }
}

```

**Как это работает "под капотом"?** Сама по себе аннотация `MustTest` ничего не делает во время выполнения. Однако, **внешний инструмент (например, анализатор кода)** может сканировать ваш проект, находить все места с `@MustTest` и, например, сгенерировать отчет или принудительно требовать тесты. Это основа для множества библиотек в Dart/Flutter (например, Freezed, JsonSerializable).

### 4. Ключевые слова `assert` и `late`

Хотя они не связаны напрямую с ошибками и метаданными, они важны для контроля качества кода.

#### А. `assert` (Утверждение)

- **Назначение:** Используется для проверки условий, которые должны быть истинными в **режиме отладки** (Debug Mode).
- **Особенность:** Полностью игнорируется в производственном коде (Release Mode).

```dart
// Выбросит ошибку (AssertionError) только в режиме отладки,
// если age < 0
assert(age >= 0, 'Возраст не может быть отрицательным!');

```

#### Б. `late` (Поздняя Инициализация)

- **Назначение:** Позволяет объявить не-нулевую переменную, но **отложить ее инициализацию** до момента первого использования.
- **Использование:**

1. Для полей класса, которые инициализируются в методе, а не в конструкторе.
2. Для избежания циклических зависимостей.
3. Для ленивой (Lazy) инициализации: значение вычисляется только при первом доступе.

```dart
// Переменная не может быть null, но не инициализируется сразу.
late String configuration;

void initialize() {
  configuration = 'Установлена конфигурация';
}

void main() {
  // configuration; // ОШИБКА: Если здесь вызвать до initialize()
  initialize();
  print(configuration); // OK
}

```

> **Под Капотом (`late`):** Когда вы используете `late`, Dart VM неявно добавляет проверку перед первым использованием. Если переменная не была инициализирована к этому моменту, выбрасывается исключение `LateInitializationError`.

---

## 🚀 Ваше Задание на Урок 8

1. **Создайте собственное исключение** `InvalidInputException` с полем `String inputData` для хранения неверных входных данных.
2. **Создайте функцию** `parseNumber(String input)`:

- Внутри используйте `try-catch`.
- Если `input` не может быть преобразован в число (например, `int.parse(input)` выбросит `FormatException`), **перехватите** это исключение и **выбросьте** свое `InvalidInputException`, передав туда исходные неверные данные.

3. **Создайте аннотацию** `const todo = Todo(String description);`. Класс `Todo` должен иметь `const` конструктор и поле `description`.
4. **Примените** `@todo` к методу в любом вашем предыдущем классе, чтобы указать, что этот метод нуждается в доработке (например, `@todo('Нужно добавить проверку прав')`).

Поздравляю! Мы завершили изучение фундаментальных и профессиональных основ Dart. Теперь мы готовы к **Уроку 9: Продвинутые Возможности Языка (Extensions, Records, Patterns)**, которые делают Dart 3.0 таким мощным!
