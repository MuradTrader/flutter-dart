Очень хорошо\! Теперь мы переходим к одной из самых фундаментальных концепций в программировании — **переменным** и **системе типов**.

Вот подробное объяснение материала из вашего текста:

---

## 💾 Переменные: Хранилище Данных (Variables)

Автор начинает с определения переменной, основываясь на самом слове: **переменная** (_variable_) — это то, что может **меняться** (_vary_).

### Что такое Переменная?

Переменные — это, по сути, **именованные ячейки памяти**, которые используются для **хранения данных**. Вы можете поместить туда значение, использовать его в коде, а затем, при необходимости, изменить его.

### Проблема Без Переменных (Hardcoding)

Автор показывает проблему, которая возникает при использовании "зашитых" или **жестко заданных значений** (_hardcoding_).

**Сценарий:** Вы пишете код для выполнения нескольких арифметических операций с двумя значениями: 19 и 5.

```dart
print(19);
print(5);
print(19 * 5);
print(19 + 5);
print(19 - 5);
```

**Проблема Hardcoding:**
Если позже потребуется изменить исходные значения (например, с **19** и **5** на **302** и **51**), вам придется **вручную** пройтись по **всему** коду и изменить каждое вхождение 19 и 5.

> Если у вас сотни строк кода, это **неэффективно** и чревато ошибками.

**Решение:** Вместо того чтобы вписывать значения вручную, мы **храним** их в переменных, а затем используем **имя переменной** в расчетах. Если значение изменится, нужно будет изменить его **только один раз** в месте объявления переменной.

---

## 🧱 Синтаксис Объявления Переменной в Dart

Чтобы создать переменную в Dart, используется следующий синтаксис (форма записи):

$$\text{Тип\_данных} \space \text{имя\_переменной} = \text{значение};$$

### Важные Элементы Синтаксиса:

1.  **Тип данных** (_Data Type_): Указывает, какой тип информации будет храниться (например, целое число, текст, десятичное число).
2.  **Имя переменной** (_Variable Name_): Имя, по которому вы будете обращаться к данным (например, `firstValue`).
3.  **Оператор присваивания** (`=`): Присваивает значение правой части левой части (помещает значение в переменную).
4.  **Значение** (_Value_): Данные, которые мы хотим сохранить.
5.  **Точка с запятой** (`;`): Завершает инструкцию (похожа на точку в английском предложении).

> **Точка с запятой (`;`):** Автор отмечает, что точка с запятой используется для завершения **почти каждой инструкции** в Dart (за исключением некоторых структур, например, кода внутри фигурных скобок `{}`). Она позволяет размещать несколько инструкций (например, несколько `print()` или объявления переменных) на одной строке.

---

## 🧬 Система Типов (Data Types)

Dart — **строго типизированный** язык, это означает, что каждая переменная должна иметь определенный тип данных, и этот тип, как правило, не может быть изменен.

Автор рассматривает несколько основных типов:

### 1\. Числа (Numbers)

Для хранения чисел используются два основных типа:

| Тип          | Название (англ.)          | Что Хранит                                                               | Примеры             |
| :----------- | :------------------------ | :----------------------------------------------------------------------- | :------------------ |
| **`int`**    | Integer (Целое число)     | Целые числа (положительные, отрицательные, но **без десятичной части**). | `19`, `-5`, `302`   |
| **`double`** | Double (Двойная точность) | Десятичные числа (с **плавающей точкой**).                               | `310.50`, `-102.22` |

**Пример объявления переменной типа `int`:**

```dart
int firstValue = 302;
int secondValue = 51;
```

**Преимущество переменных:**
Теперь, чтобы выполнить те же расчеты, мы используем имена переменных, а не жестко заданные значения:

```dart
print(firstValue * secondValue);
// ... и т.д.
```

Если нужно поменять 302 на 310, вы меняете **только одну строку** (`int firstValue = 310;`), и все последующие расчеты автоматически используют новое значение.

### 2\. Строка (String)

- **Тип:** **`String`**
- **Назначение:** Хранение **текста**, слов, символов.
- **Особенность:** Текст должен быть заключен в одинарные (`'`) или двойные (`"`) кавычки.

<!-- end list -->

```dart
String greeting = "Hello world";
```

### 3\. Булево Значение (Boolean)

- **Тип:** **`bool`** (сокращение от **Boolean**)
- **Назначение:** Хранение только двух ключевых логических значений: **`true`** (истина) или **`false`** (ложь).
- **Пример использования:** Проверка условий, например, "Является ли взрослым?"

<!-- end list -->

```dart
bool isAdult = true;
bool isChild = false; // Конвенция именования: часто начинается с "is"
```

### 4\. Динамический Тип (Dynamic)

- **Тип:** **`dynamic`**
- **Назначение:** Переменная, которая может хранить **любой** тип данных (`int`, `double`, `String`, `bool`) и может **менять свой тип** во время выполнения.

<!-- end list -->

```dart
dynamic someValue = 10.5; // Сначала double
someValue = "Hello";      // Потом String
```

**Важное предупреждение автора:**
Хотя `dynamic` удобен, его **не рекомендуется использовать** без крайней необходимости.

- **Причина:** Когда вы указываете `int` или `String`, система Dart знает, какие операции и свойства доступны для этого типа (например, `.length` для строк или `.isEven` для целых чисел). Когда тип `dynamic`, Dart не знает, что вы храните, и **откладывает проверку ошибок до момента выполнения** (_runtime_).
- Это может привести к **ошибкам во время работы программы**, которые невозможно было обнаружить во время написания кода (_compile-time_).

> **Пример:** Если `someValue` объявлена как `dynamic` и содержит строку `"Hello"`, но вы пытаетесь написать `someValue + 3`, Dart не выдаст ошибку во время написания кода. Ошибка возникнет только при попытке запустить программу, потому что нельзя сложить строку и число. Если бы `someValue` была `String`, ошибка была бы видна сразу.

---

## ✍️ Именование Переменных (Naming Conventions)

Автор затрагивает важные правила и конвенции:

1.  **Уникальность:** Вы не можете объявить две переменные с **одинаковым** именем в одной области видимости.
    - **Пример ошибки:** `int firstValue = 20;` и затем снова `int firstValue = 30;` – это ошибка.
2.  **Case-Sensitivity (Чувствительность к регистру):** Dart **чувствителен к регистру**. `firstValue` и `FirstValue` — это две **разные** переменные.
3.  **Читаемость:** Имена должны быть **описательными** (`firstValue`, `isAdult`), а не сокращениями (`fv`).
4.  **Camel Case (Верблюжий регистр):** Это **общепринятая конвенция** (_convention_) для именования переменных:
    - Первое слово пишется с **маленькой буквы**.
    - Каждое последующее слово начинается с **заглавной буквы**.
    - **Пример:** `someValue`, `firstValue`, `isNotAdult`.

---

## 🔄 Изменение Значения Переменной (Reassignment)

Переменные могут менять свое значение. Этот процесс называется **переприсваиванием** (_reassignment_).

### 1\. Простое Переприсваивание

Чтобы изменить значение, не нужно указывать тип данных снова:

```dart
int firstValue = 25; // Создали переменную
print(firstValue);  // Вывод: 25

firstValue = 100;    // Переприсвоили новое значение (БЕЗ int!)
print(firstValue);  // Вывод: 100
```

### 2\. Изменение Значения с Помощью Операторов (Shorthand Syntax)

Очень часто требуется изменить значение, основываясь на его текущем значении (например, увеличить на 75).

**Полный синтаксис:**

```dart
// Увеличить firstValue на 75
firstValue = firstValue + 75;
```

**Сокращенный синтаксис (Shorthand):**

Это более компактный и предпочтительный способ:

```dart
// Это то же самое, что: firstValue = firstValue + 75;
firstValue += 75;

// Сокращенные операторы:
firstValue -= 75; // firstValue = firstValue - 75;
firstValue *= 75; // firstValue = firstValue * 75;
firstValue /= 75; // firstValue = firstValue / 75;
```

---

## ⛓️ Особенности Работы со Строками

### 1\. Конкатенация (Объединение) Строк

Хотя строки можно объединять с помощью оператора `+` (см. предыдущий урок: `"Hello" + " world"`), автор предупреждает, что для Dart (и многих других языков) предпочтительным способом является **интерполяция строк**.

### 2\. Интерполяция Строк (String Interpolation)

**Интерполяция** — это вставка значения переменной (или результата выражения) внутрь строки, используя специальный синтаксис: **знак доллара** (`$`).

#### Использование с одиночной переменной

Если вы вставляете **только имя переменной**, достаточно знака доллара:

```dart
String greeting = "Hello";
print("Привет, $greeting!"); // Вывод: Привет, Hello!
```

#### Использование с выражениями

Если вы хотите вставить что-то **сложнее, чем просто имя переменной** (например, результат операции или свойство переменной, такое как `.length`), необходимо заключить выражение в **фигурные скобки**:

```dart
String greeting = "Hello world";
// Чтобы получить длину строки внутри другой строки:
print("Длина строки: ${greeting.length}"); // Вывод: Длина строки: 11
```

> **Правило:** Если после `$` идет **одно слово** (имя переменной), скобки не нужны. Если идет **выражение** (с точками, операциями и т.д.), скобки `{}` **обязательны**.

### 3\. Специальные Символы (Escape Sequences)

Некоторые комбинации символов имеют особое значение в строках:

- **`\n` (New Line):** Создает переход на новую строку.
  ```dart
  print("Hello\nworld");
  // Вывод:
  // Hello
  // world
  ```
- **Экранирование доллара (`\$`):** Если вы хотите, чтобы символ `$` отображался как обычный символ доллара, а не как начало интерполяции, нужно поставить перед ним обратный слэш (`\`).
  ```dart
  print("Цена: \$12"); // Вывод: Цена: $12
  ```

### 4\. Многострочные Строки (Multi-line Strings)

Для хранения длинного текста, который охватывает несколько строк кода, используются **три одинарные или три двойные кавычки**:

```dart
String longText = '''
  Это первая строка.
  Это вторая строка.
''';
```

---

## 🎯 Типовая Система Dart (Type System)

Автор завершает обсуждением **типовой системы** Dart, которая обеспечивает его надежность.

Типовая система Dart не позволяет переменной **менять свой тип** после первого присваивания (если она не была объявлена как `dynamic`).

- Если вы объявили `String greeting = "Hello";`, вы **не можете** позже присвоить ей целое число: `greeting = 10;` — это вызовет **ошибку компиляции** (видна во время написания кода).

> **Преимущество:** Эта строгость (`int` должен оставаться `int`, `String` должен оставаться `String`) помогает отлавливать большую часть ошибок **до запуска** программы, делая код более предсказуемым и надежным.
