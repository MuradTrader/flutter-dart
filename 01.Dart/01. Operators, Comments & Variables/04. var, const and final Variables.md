Отлично\! Теперь мы переходим к более современным и гибким способам объявления переменных в Dart, а также к концепции **изменяемости** и **неизменяемости**.

Вот подробное объяснение того, что рассказывает автор о ключевых словах `var`, `final` и `const`.

---

## 🔑 Ключевые слова `var`, `final` и `const`

Автор представляет альтернативный синтаксис для создания переменных, который не требует явного указания типа данных (`int`, `String` и т.д.):

$$\text{Ключевое\_слово} \space \text{имя\_переменной} = \text{значение};$$

В качестве $\text{Ключевого\_слова}$ выступает одно из трех: **`var`**, **`final`** или **`const`**.

### 1\. `var` (Variable)

Ключевое слово `var` используется для объявления переменной, чей **тип данных** определяется автоматически, основываясь на её **начальном значении**.

```dart
var someValue = 10;
```

**Как работает `var`:**

1.  **Вывод типа (Type Inference):** Благодаря **мощной системе типов Dart** (_Dart's powerful type system_), или точнее, **системе вывода типов** (_type inference system_), Dart **автоматически** определяет, что `10` — это целое число.
2.  **Результат:** Переменная `someValue` ведет себя **точно так же**, как если бы вы написали `int someValue = 10;`.
3.  **Не является `dynamic`:** Автор подчеркивает, что `var` — **это не `dynamic`**. После того как тип определен (в данном случае `int`), он **фиксируется** на всю жизнь переменной.
    - Если вы попытаетесь изменить `someValue` на строку (`someValue = "hello";`), Dart выдаст ошибку компиляции, поскольку переменная уже имеет фиксированный тип `int`.
    - В документации или при автодополнении IDE вы увидите, что тип переменной — `int`, а не `var`.

**Основная мысль:** `var` позволяет не писать тип данных (например, `int`), но переменная **по-прежнему строго типизирована**.

---

## 🛡️ Различия в Изменяемости (`var` vs `final` vs `const`)

Основное различие между этими тремя ключевыми словами заключается в их **изменяемости** (_mutability_) или **неизменяемости** (_immutability_).

### A. `var` (Mutable — Изменяемая)

- **Назначение:** Обычная переменная, значение которой **можно** менять (переприсваивать) после создания.
- **Термин:** **Изменяемая** (_Mutable_).
- **Поведение:**
  ```dart
  var someValue1 = 10;
  someValue1 = 10001; // ✅ OK. Значение меняется.
  ```

### B. `final` (Immutable — Неизменяемая в Runtime)

- **Назначение:** Переменная, значение которой **может быть присвоено только один раз**. Значение устанавливается при создании и **не может быть изменено** (переприсвоено) позже.
- **Термин:** **Неизменяемая** (_Immutable_).
- **Поведение:**
  ```dart
  final someValue2 = 10;
  // someValue2 = 10001; // ❌ Ошибка: final variable can only be set once.
  ```
- **Важное отличие (от `const`):** Значение `final` переменной не обязательно должно быть известно **во время компиляции**. Оно может быть вычислено или получено **во время выполнения** программы (_runtime_).

### C. `const` (Immutable — Неизменяемая в Compile Time)

- **Назначение:** Переменная, значение которой **должно быть известно во время компиляции** (то есть до того, как программа начнет работать). Это означает, что `const` не может зависеть от каких-либо вычислений или данных, полученных во время выполнения.
- **Термин:** **Константа** (_Constant_) или **Неизменяемая** (_Immutable_).
- **Поведение:**
  ```dart
  const someValue3 = 10;
  // someValue3 = 10001; // ❌ Ошибка: Constant variables cannot be assigned a value.
  ```
- **Важное отличие (от `final`):** `const` гарантирует, что значение **никогда не изменится** и **будет фиксировано еще до запуска** программы.

---

## 📝 Сравнение и Итоговые Концепции

Автор суммирует основные различия:

| Ключевое слово | Изменяемость (Mutability)    | Переприсваивание (Reassignment) | Когда известно значение                                         |
| :------------- | :--------------------------- | :------------------------------ | :-------------------------------------------------------------- |
| **`var`**      | **Mutable** (Изменяемое)     | **Разрешено**                   | Присваивается во время выполнения (_Runtime_).                  |
| **`final`**    | **Immutable** (Неизменяемое) | **Запрещено** (только 1 раз)    | Присваивается во время выполнения (_Runtime_), но только 1 раз. |
| **`const`**    | **Immutable** (Неизменяемое) | **Запрещено** (только 1 раз)    | **Должно быть известно** во время компиляции (_Compile Time_).  |

### Мутабельность и Иммутабельность

Автор вводит важные термины:

- **Мутабельность (Mutability):** Свойство переменной, которая **может быть изменена** после присваивания (`var`).
- **Иммутабельность (Immutability):** Свойство переменной, которая **не может быть изменена** после присваивания (`final` и `const`).

### Правила Именования Переменных

Автор напоминает о важном правиле именования:

- Имена переменных (например, `someValue1`) **могут** содержать числа (цифры).
- Однако имя переменной **не может начинаться с цифры** (например, `1someValue` — не разрешено).
