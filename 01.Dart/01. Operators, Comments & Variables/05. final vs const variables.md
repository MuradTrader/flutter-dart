Отлично\! Автор переходит к тонкому, но критически важному различию между **`final`** и **`const`** — различие, которое часто сбивает с толку новичков.

Вот подробное объяснение этого ключевого момента:

---

## 🆚 Сравнение `final` и `const`: Константы Времени Выполнения и Времени Компиляции

Автор верно отмечает, что и `final`, и `const` создают **неизменяемые** (_immutable_) переменные (их значение нельзя изменить после присваивания). Однако они были созданы как два разных ключевых слова, потому что представляют константы, фиксирующиеся в **разные моменты времени** в жизненном цикле программы.

### 1\. `const` (Compile-Time Constant)

- **Полное название:** Константа времени компиляции.

- **Когда фиксируется значение:** Значение должно быть **абсолютно известно** до того, как программа начнет выполняться (на этапе **компиляции**).

- **Что можно присваивать:** Только **литералы** (например, `10`, `"Hello"`, `true`) или другие **`const`** переменные/выражения.

- **Пример:**

  ```dart
  const compileTimeValue = 10; // 10 известно до запуска
  const String name = "Alice";  // "Alice" известно до запуска
  ```

### 2\. `final` (Runtime Constant)

- **Полное название:** Константа времени выполнения.

- **Когда фиксируется значение:** Значение не обязательно должно быть известно заранее, но оно **вычисляется только один раз** в момент первого использования переменной (на этапе **выполнения**).

- **Что можно присваивать:** Любые допустимые значения, включая те, которые зависят от состояния системы или вычислений во время работы программы.

- **Пример:**

  ```dart
  final runTimeValue = 10 + 20; // Вычисляется при первом запуске
  ```

---

## ⏰ Демонстрация Различия: `DateTime.now()`

Автор использует объект **`DateTime.now()`** для идеальной демонстрации этого различия.

**Что делает `DateTime.now()`:**
Это функция (метод), которая возвращает текущие дату и время **в момент ее вызова** (т.е. когда программа **уже запущена**).

### A. Поведение с `const` (Ошибка компиляции)

Когда автор пытается присвоить результат `DateTime.now()` переменной `const`:

```dart
const someValue3 = DateTime.now(); // ❌ ОШИБКА!
```

**Причина ошибки:**

1.  **`const`** требует, чтобы значение было известно на этапе **компиляции** (до запуска).
2.  **`DateTime.now()`** — это "переменное" значение (текущее время постоянно меняется), и его нельзя узнать, пока программа **не начнет выполняться**.
3.  Dart выдает **ошибку компиляции** (вы увидите ее прямо в редакторе кода), потому что условие `const` нарушено.

### B. Поведение с `final` (Успешное выполнение)

Когда автор присваивает результат `DateTime.now()` переменной `final`:

```dart
final someValue2 = DateTime.now(); // ✅ ОК!
```

**Почему это работает:**

1.  **`final`** требует, чтобы значение было присвоено **только один раз** во время жизни переменной.
2.  Значение `DateTime.now()` вычисляется **один раз** в момент запуска кода. Это значение фиксируется в `someValue2` и **никогда не меняется** в дальнейшем.
3.  Поскольку значение фиксируется во время **выполнения** (_runtime_), а не компиляции, использование `final` разрешено.

## 💡 Вывод и Конвенции

1.  **`final`** используется для значений, которые **фиксируются при первом использовании** и больше не меняются (например, текущее время, данные, загруженные из файла или сети).
2.  **`const`** используется для значений, которые являются **неизменными константами** и известны еще до запуска программы (например, математические константы, фиксированные строки, числа).

### Преимущество `var`

Автор возвращается к тому, почему разработчики предпочитают использовать `var`, а не явное указание типа (например, `int someValue = 10;`):

- **Удобство:** Если Dart может автоматически определить тип (`var someValue = 10;` $\rightarrow$ `int someValue = 10;`), нет необходимости печатать тип вручную. Это экономит время и делает код чище.
- **Конвенция:** В современном Dart это стало общепринятой конвенцией: использовать **`var`** для изменяемых переменных и **`final`** (или **`const`**) для неизменяемых, позволяя Dart выполнить вывод типа.

> **Важно:** Вы всегда можете явно указать тип (`final DateTime date = DateTime.now();`), и это не будет ошибкой. Но если система вывода типов работает, большинство разработчиков предпочитают писать меньше.
