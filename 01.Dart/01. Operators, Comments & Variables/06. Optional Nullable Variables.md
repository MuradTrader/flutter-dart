Привет\! Мы переходим к одной из самых важных особенностей современного Dart, которая обеспечивает безопасность и надежность кода: **обнуляемости** (Null Safety).

Вот подробное объяснение концепций **nullable variables** и **sound null safety**.

---

## ❓ Зачем Нужны Явные Типы (`String`, `int` и т.д.)

Автор объясняет, почему он сначала показал явное объявление типов (`String someValue = "Hello";`), прежде чем перейти к гибким ключевым словам (`var`, `final`, `const`):

1.  **Обучение типам:** Чтобы вы понимали, что такое **строка** (`String`), **целое число** (`int`) и другие типы.
2.  **Переприсваивание:** Чтобы показать, как изменять значения переменных.
3.  **Типовая безопасность (Type Safety):** Чтобы продемонстрировать, что Dart строго следит за тем, чтобы вы не присваивали один тип данных другому (например, строку — переменной типа `int`).

> **Ключевая идея:** Использование `var`/`final`/`const` не отменяет необходимость знать базовые типы. Эти ключевые слова просто позволяют Dart **автоматически выводить тип** за вас.

---

## 🚫 Null Safety: Обнуляемые (Optional) Переменные

Этот раздел посвящен концепции **обнуляемости** (_nullability_), которая была введена в Dart 2.

### Что такое `null`?

**`null`** — это специальное ключевое слово и значение, которое означает **"отсутствие значения"** или **"ничто"**.

- Важно: **`null` не равно нулю (`0`)** и **не равно пустой строке (`""`)**.
  - `int someInt = 0;` $\rightarrow$ Переменная имеет значение `0`.
  - `String someString = "";` $\rightarrow$ Переменная имеет значение "пустая строка" (состоящее из 0 символов).
  - **`null`** $\rightarrow$ Переменная вообще не содержит значения.

### Проблема До Dart 2.0

В старых версиях Dart любая переменная могла быть `null` по умолчанию. Это приводило к частым ошибкам времени выполнения (_runtime errors_), когда код пытался использовать метод для переменной, которая на самом деле была `null` (т.е. отсутствовала).

### Решение: **Sound Null Safety** (Надежная безопасность от `null`)

Начиная с Dart 2, все переменные **не могут** быть `null` по умолчанию.

**Пример:**

```dart
int sum; // ❌ ОШИБКА! sum не может быть null.
int sum = 0; // ✅ ОК.
```

### Как Разрешить Переменной Быть `null`

Чтобы явно разрешить переменной содержать значение `null` (сделать ее **обнуляемой**), необходимо добавить **знак вопроса** (`?`) после типа данных.

$$\text{Тип\_данных} \mathbf{?} \space \text{имя\_переменной} = \text{значение};$$

**Примеры:**

```dart
String? someValue = null; // ✅ ОК: Может быть строкой или null
int? someInt;             // ✅ ОК: Может быть int или null (по умолчанию null)
```

> **Важно:** Как отмечает автор, даже если вы не присваиваете обнуляемой переменной значение, она **автоматически** инициализируется значением **`null`** (поэтому присваивание `= null` является **избыточным** — _redundant initialization_).

### Почему Не Стоит Использовать `final` или `var` с `null`

Если вы попытаетесь объявить переменную, которая должна быть `null`, используя `var` или `final`:

```dart
var someValue = null;
final someOtherValue = null;
```

Dart не может вывести тип из `null`, поэтому он присваивает переменной нежелательный **динамический тип** (`dynamic`). Как вы уже знаете, **`dynamic`** убирает типовую безопасность и его следует избегать.

> **Вывод:** Когда вы хотите создать **обнуляемую** переменную, всегда **явно указывайте тип** со знаком вопроса (`String?`, `int?`).

---

## 🛡️ Null-Aware Операторы (Операторы, Осведомленные о `null`)

Самая важная часть Null Safety — это предотвращение ошибок. Когда компилятор Dart видит обнуляемую переменную (`String?`), он **запрещает** использовать её методы (например, `.length`), пока вы не докажете, что она не `null`.

### 1\. Условный Вызов (`?.`)

Этот оператор используется для безопасного доступа к свойствам или методам обнуляемого объекта.

- **Синтаксис:** `переменная?.метод`
- **Логика:** **"Если переменная не `null`, вызови метод. Иначе верни `null`."**

**Пример:**

```dart
String? greeting = null;
print(greeting?.length); // Вывод: null (ошибки нет, потому что greeting == null)

greeting = "Hello";
print(greeting?.length); // Вывод: 5 (безопасно вызван .length)
```

Если бы вы использовали `greeting.length` напрямую, Dart выдал бы ошибку компиляции (т.к. `greeting` _может_ быть `null`).

### 2\. Оператор "Если `null`, то" (`??`)

Этот оператор (называемый **оператором объединения `null`**) используется, чтобы предоставить **значение по умолчанию**, если левая часть выражения оказывается `null`.

- **Синтаксис:** `выражение ?? значение_по_умолчанию`
- **Логика:** **"Если `выражение` не `null`, используй его. Иначе используй `значение_по\_умолчанию`."**

**Пример:** Вы хотите вывести длину строки, но если строка `null`, выводите `0` (как предлагает автор):

```dart
String? greeting = null;

// Используем ?. для безопасного доступа, а затем ?? для значения по умолчанию
print(greeting?.length ?? 0); // Вывод: 0

greeting = "Hi";
print(greeting?.length ?? 0); // Вывод: 2
```

### 3\. Оператор Принудительного Развертывания (`!`)

Этот оператор (также называемый **"bang" оператором** или **"null-assertion" оператором**) используется, чтобы **сообщить компилятору**, что вы **уверены**, что переменная не `null`, даже если она обнуляемая.

- **Синтаксис:** `переменная!.метод`
- **Логика:** **"Доверяй мне, компилятор, эта переменная НЕ `null`."**

**Пример:**

```dart
String? greeting = "Hello";
print(greeting!.length); // ✅ OK. Если greeting != null, работает

greeting = null;
print(greeting!.length); // 💥 ОШИБКА ВРЕМЕНИ ВЫПОЛНЕНИЯ (RUNTIME ERROR)!
// Выдает Uncaught TypeError: Cannot read properties of null.
```

**Важное предупреждение автора:**
Оператор `!` убирает безопасность. Если вы используете `!`, а переменная на самом деле `null`, вы получите **ошибку во время работы программы**, что гораздо хуже, чем ошибка компиляции. Используйте `!` только тогда, когда вы абсолютно уверены.

---

## 🌟 Sound Null Safety: Контекст Функций

Автор приводит пример, когда `String? someValue` находится **внутри** функции `main()`, и компилятор **уже знает** о её возможных `null` значениях:

1.  **Внутри `main()`:** Компилятор может "проследить" за переменной и знает, что в строке 10 она `null`, а в строке 15 она `"Hello"`. Благодаря этому компилятор может иногда **отключать** предупреждения о ненужном `?.` или, наоборот, **точно указать** на ошибку.
2.  **Вне `main()` (глобальная переменная):** Когда переменная объявлена **вне** функции, компилятор не может "проследить" её путь и контекст использования. В этом случае он более **строг** и требует от вас явного использования null-aware операторов (`?.`, `??`) для обеспечения безопасности, поскольку он **не уверен** в ее текущем значении.

**Основная мысль:** **Sound Null Safety** — это способность Dart (его компилятора) отслеживать обнуляемые значения по **всему коду** и использовать эту информацию для предотвращения ошибок, максимально перенося их из времени выполнения в время компиляции.

> **Кратко об операторах:**
>
> - `?` $\rightarrow$ Объявление переменной как обнуляемой.
> - `?.` $\rightarrow$ Выполнить метод/доступ, только если не `null`.
> - `??` $\rightarrow$ Использовать значение справа, если левая часть `null`.
> - `!` $\rightarrow$ Принудительно сказать компилятору: "Она точно не `null`\!" (использовать с осторожностью).
