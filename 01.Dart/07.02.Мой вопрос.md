# Мой вопрос

```
Я понял пункт с Future так как я отлично знаю язык JS/TS и Future очень похож на Promise,
но я не понял что такое Stream объясни максимально подробно
```

Это отличное наблюдение! Вы правы, **`Future` в Dart** функционально эквивалентен **`Promise` в JavaScript/TypeScript** (или `Task` в C#, `Deferred` в некоторых других языках). Оба представляют **одно** асинхронное значение, которое будет доступно в будущем.

Теперь давайте максимально подробно разберем **`Stream`** — концепцию, которая может быть менее очевидна, если вы привыкли только к `Promise`.

---

## 🌊 Подробное Объяснение `Stream` в Dart

`Stream` в Dart похож на **непрерывный поток данных** или **серию событий** в течение времени.

Если **`Future`** можно сравнить с **единичной доставкой** (например, вы заказываете пиццу, и она придет один раз), то **`Stream`** можно сравнить с **подпиской на газету**, где вам регулярно (несколько раз) доставляют новый выпуск.

### 1. Фундаментальное Отличие: Future vs Stream

| Характеристика         | `Future<T>`                             | `Stream<T>`                                                           |
| ---------------------- | --------------------------------------- | --------------------------------------------------------------------- |
| **Что представляет?**  | Одно асинхронное значение (или ошибку). | Серию (последовательность) асинхронных значений (или ошибок).         |
| **Когда завершается?** | Завершается, когда значение доступно.   | Завершается, когда нет больше событий для отправки.                   |
| **Типичные примеры**   | Получение данных из API, чтение файла.  | Клики пользователя, изменения в базе данных, входящие сетевые пакеты. |

### 2. Анатомия Stream: Издатель и Подписчик

Stream работает по паттерну **Издатель-Подписчик (Publisher-Subscriber)**:

1. **Издатель (Source/Stream):** Это источник событий (данных). Он "излучает" (emits) события.
2. **Подписчик (Listener):** Это код, который "слушает" (listens) этот поток. Он реагирует на каждое событие по мере его поступления.

Чтобы начать получать данные, вы должны **подписаться** на Stream.

```dart
// 1. Подписка: начинаем слушать поток
myStream.listen(
  (data) { // Вызывается при получении каждого нового значения
    print('Новое значение: $data');
  },
  onError: (error) { // Вызывается при ошибке
    print('Ошибка в потоке: $error');
  },
  onDone: () { // Вызывается, когда поток данных исчерпан (закрыт)
    print('Поток завершен.');
  }
);

```

### 3. Типы Stream: Одноразовые и Широковещательные

Это важная деталь "под капотом", о которой спрашивают на собеседованиях.

#### А. Single-Subscription Stream (Одноразовый Поток)

- **Назначение:** Предназначен только для **одного** подписчика.
- **Правило:** Как только первый подписчик завершает прослушивание (отписывается), поток закрывается. Если попытаться подписаться на него снова, это приведет к ошибке.
- **Примеры:** Большинство потоков ввода/вывода (чтение файла, HTTP-ответ), так как эти данные обычно читаются только один раз.

#### Б. Broadcast Stream (Широковещательный Поток)

- **Назначение:** Предназначен для **множества** подписчиков.
- **Правило:** Несколько подписчиков могут слушать один и тот же поток одновременно или в разное время. Поток остается открытым, пока его не закроет источник.
- **Примеры:** События пользовательского интерфейса (клики мыши), системные уведомления.

**Как создать Broadcast Stream?** Используйте метод `asBroadcastStream()`:

```dart
Stream<int> singleStream = Stream.periodic(Duration(seconds: 1), (i) => i).take(5);

// Превращаем его в широковещательный
Stream<int> broadcastStream = singleStream.asBroadcastStream();

broadcastStream.listen((n) => print('Подписчик 1: $n'));
broadcastStream.listen((n) => print('Подписчик 2: $n')); // OK

```

### 4. Создание Stream

Существует два основных способа создания Stream:

#### А. Генератор Потока (Async Generator) — `async*` и `yield`

Это самый современный и чистый способ. Функция, помеченная `async*` (со звездочкой), **автоматически возвращает `Stream**`. Вместо `return` используется ключевое слово **`yield`\*\* для "выброса" значения в поток.

```dart
// Эта функция генерирует поток целых чисел
Stream<int> counter() async* {
  for (int i = 1; i <= 3; i++) {
    await Future.delayed(Duration(seconds: 1)); // Имитация асинхронной работы
    yield i; // Отправляем значение i в поток
  }
  // После завершения цикла, поток автоматически закрывается
}

```

#### Б. Контроллеры Потока (StreamController)

Для более сложного управления потоком (например, для создания Broadcast Stream, для ручного добавления данных из разных мест) используется класс `StreamController`.

```dart
import 'dart:async';

// Создаем контроллер для потока чисел
final StreamController<int> controller = StreamController<int>();

void startSendingData() {
  controller.sink.add(10); // Добавляем данные в "раковину" (sink) потока
  controller.sink.add(20);

  // Внешний код может также добавить ошибку
  // controller.sink.addError('Connection Lost');

  controller.close(); // Обязательно закрываем контроллер, когда данные закончились
}

void main() {
  controller.stream.listen((data) => print('Контроллер: $data'));
  startSendingData();
}

```

> **Под Капотом (Sink):** Объект `sink` (раковина) — это входной канал для добавления данных, ошибок или закрытия потока. Объект `stream` — это выходной канал для прослушивания данных.

### 5. Обработка Stream: `await for`

Как мы видели, традиционный метод — это `.listen()`. Однако, для написания более последовательного кода в асинхронной функции (`async`), Dart предлагает **асинхронный цикл `await for**`.

```dart
Future<void> processCounter() async {
  // Асинхронный цикл ждет, пока counter() не выдаст следующее значение.
  // Пока он ждет, основной поток свободен.
  await for (final number in counter()) {
    print('Обработано в цикле: $number');
  }
  print('Обработка потока завершена.');
}

void main() {
  processCounter();
}
// Вывод будет:
// Обработано в цикле: 1 (через 1 сек)
// Обработано в цикле: 2 (через 1 сек)
// Обработано в цикле: 3 (через 1 сек)
// Обработка потока завершена.

```

`await for` — это элегантный способ "развернуть" события Stream в последовательном порядке.

### 6. Функциональные Операции со Stream

Как и в случае с `List`, Stream имеет мощные функциональные методы, которые создают новый Stream на основе старого:

| Метод           | Назначение                                                                  | Пример                                                                   |
| --------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| **`.map()`**    | Преобразует каждое поступающее значение.                                    | `stream.map((n) => n * 2)` новый поток с удвоенными значениями.          |
| **`.where()`**  | Фильтрует значения.                                                         | `stream.where((n) => n.isEven)` новый поток только с четными значениями. |
| **`.take(n)`**  | Берет только первые `n` элементов и закрывает поток.                        | `stream.take(5)` ограничивает поток пятью событиями.                     |
| **`.toList()`** | Собирает все значения потока в один `List` и возвращает **`Future<List>`**. | `await stream.toList()` ожидание завершения потока и получение списка.   |

---

## 🚀 Ваше Задание для Закрепления Stream

1. **Создайте широковещательный Stream** с помощью `StreamController.broadcast()`.
2. Создайте две функции:

- `addTemperature(int temp)`: Добавляет значение температуры в поток.
- `closeStream()`: Закрывает поток.

3. **Подпишитесь** на этот поток **дважды** в функции `main()` (для демонстрации широковещательности):

- Первый подписчик выводит все значения.
- Второй подписчик использует `.where()` для вывода только температуры выше 25 градусов.

4. В `main()`, вызовите `addTemperature` несколько раз (например, 20, 30, 22, 28) и затем вызовите `closeStream()`.

Как только вы справитесь с этим, мы перейдем к **Уроку 8: Обработка Ошибок, Исключения и Метаданные (Аннотации)**.
