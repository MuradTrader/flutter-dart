Прекрасно! Вы освоили все классические и профессиональные основы Dart, включая ООП, асинхронность и обработку ошибок.

**Урок 9** посвящен самым современным и мощным функциям, представленным в Dart 3.0. Эти возможности делают код более чистым, выразительным и безопасным, и они **обязательны** для любого, кто хочет называться профессиональным разработчиком Dart.

## 📚 Урок 9: Продвинутые Возможности Языка (Extensions, Records, Patterns)

### 1. Методы Расширения (Extension Methods)

**Методы расширения** позволяют "добавить" новую функциональность (методы и геттеры/сеттеры) к **уже существующим классам**, даже если вы не имеете доступа к исходному коду этого класса (например, классы из Dart SDK, такие как `String`, `int`, `List`).

#### А. Синтаксис

Вы объявляете расширение с помощью ключевого слова **`extension`**, указывая имя расширения и тип, который оно расширяет. Внутри методов используется ключевое слово **`this`** для ссылки на объект, который вызывал метод.

```dart
// Расширение для класса String
extension StringUtils on String {
  // Геттер: возвращает строку с заглавной первой буквой
  String get capitalize {
    if (this.isEmpty) return this;
    return this[0].toUpperCase() + this.substring(1);
  }

  // Метод: проверяет, является ли строка числом
  bool isNumeric() {
    return double.tryParse(this) != null;
  }
}

void main() {
  String name = 'dart programming';
  print(name.capitalize); // Вывод: Dart programming

  String number = '42';
  print(number.isNumeric()); // Вывод: true
}

```

> **Под Капотом:** Расширения — это синтаксический сахар. Компилятор Dart на самом деле не изменяет исходный класс. Вместо этого, он преобразует вызов `name.capitalize` в статический вызов функции, например, `StringUtils.capitalize(name)`. Это означает, что **нет накладных расходов во время выполнения**.

### 2. Записи (Records)

**Записи** — это новая, крайне полезная функция, которая позволяет объединять несколько объектов в один простой, неизменяемый и анонимный составной тип. Они идеальны для случаев, когда функциям нужно вернуть несколько значений без необходимости объявлять отдельный класс.

#### А. Синтаксис и Возврат Нескольких Значений

Записи используют круглые скобки `()`.

```dart
// Функция, которая возвращает и имя пользователя, и статус его подписки
(String, bool) fetchUserDetails(int id) {
  // Позиционные поля
  return ('Анна', true);
}

// Функция, которая возвращает именованные поля (читаемость лучше)
({String name, int code}) fetchError() {
  // Именованные поля
  return (name: 'Not Found', code: 404);
}

void main() {
  // 1. Деструктуризация позиционной записи
  var (username, isSubscribed) = fetchUserDetails(1);
  print('Пользователь: $username, Подписка: $isSubscribed');

  // 2. Доступ к полям по позиции ($1, $2)
  var details = fetchUserDetails(2);
  print(details.$1); // Доступ к первому полю

  // 3. Доступ к именованным полям
  var error = fetchError();
  print('Ошибка: ${error.code} - ${error.name}');
}

```

> **Сравнение с List:** В отличие от `List`, записи **типобезопасны**. `(String, int)` и `(int, String)` — это разные типы, и компилятор будет проверять их.

### 3. Сопоставление Шаблонов (Pattern Matching)

**Сопоставление шаблонов** — это мощная функция, позволяющая декомпозировать (разобрать) объекты, коллекции и записи и выполнять код в зависимости от их формы, типа или значения. Это значительно упрощает сложную логику `if-else` или `switch`.

#### А. Сопоставление с Помощью `switch` (Switch Expressions/Statements)

В Dart 3 `switch` стал гораздо более гибким и может быть использован как выражение (которое возвращает значение).

```dart
// Пример 1: Сопоставление по типу
String handle(Object value) {
  return switch (value) {
    // Шаблон типа (Type Pattern)
    int i => 'Целое число: $i',
    String s => 'Строка с длиной: ${s.length}',

    // Шаблон записи (Record Pattern)
    (int code, String message) => 'Код: $code, Сообщение: $message',

    _ => 'Неизвестный тип', // Wildcard (подходит ко всему)
  };
}

void main() {
  print(handle(10)); // Вывод: Целое число: 10
  print(handle(('E404', 'Error'))); // Вывод: Код: E404, Сообщение: Error
}

```

#### Б. Сопоставление в Объявлениях (Variable Declarations)

Вы можете использовать сопоставление для декомпозиции объектов прямо в объявлении переменной.

```dart
List<String> coordinates = ['10', '20', '30'];

// Декомпозиция списка:
// first получает '10', second получает '20', rest получает ['30']
var [first, second, ...rest] = coordinates;
print('Первый: $first, Остаток: $rest'); // Вывод: Первый: 10, Остаток: [30]

// Сопоставление именованных полей объекта (если бы у нас был класс Point)
// if (point case Point(x: var x, y: var y)) {
//   print('Координаты: $x, $y');
// }

```

#### В. Сопоставление `if-case`

Используется для проверки, соответствует ли объект конкретному шаблону.

```dart
Object httpResponse = (403, 'Forbidden'); // Это Record

// Проверяем, является ли httpResponse записью с int и String,
// и сохраняем их значения в code и message.
if (httpResponse case (int code, String message)) {
  print('Проверка пройдена. HTTP: $code');
} else {
  print('Некорректный ответ.');
}

```

> **Под Капотом (Patterns):** Patterns невероятно важны для Flutter. Они позволяют писать очень выразительный код для UI, где вы можете проверить состояние (`Loading`, `Success`, `Error`) и сразу декомпозировать необходимые данные, избегая каскадов `if (state is Loading) {} else if (state is Success) {}`.

---

## 🚀 Ваше Задание на Урок 9

1. **Методы Расширения:** Создайте расширение для класса `List<int>`. Добавьте метод `sum()` (Геттер или метод), который возвращает сумму всех элементов списка.
2. **Записи (Records):** Напишите функцию `getUserData(int id)`:

- Она должна возвращать **запись** с именованными полями: `({String username, String role, int level})`.
- Имитируйте возврат данных, например, `(username: 'Hero', role: 'Admin', level: 99)`.

3. **Сопоставление Шаблонов:**

- В `main()`, вызовите `getUserData()` и используйте **декомпозицию записи** (`var (...) = ...;`) для извлечения только `username` и `level` (игнорируя `role`).
- Создайте функцию `describeUserStatus(record)`:
- Используйте **`switch` выражение** на записи, возвращаемой `getUserData`.
- Если `level` больше 90, вернуть `'Элитный Администратор'`.
- Если `role` равен `'Admin'`, но `level` меньше 90, вернуть `'Обычный Администратор'`.
- В противном случае вернуть `'Пользователь'`.

Поздравляю, вы освоили самый актуальный и продвинутый Dart! Мы закончили полный курс по языку. В следующем, **Финальном Уроке**, мы обсудим **Пакеты (Pub), Импорты и Структуру Проекта**, что необходимо для профессионального развертывания кода.
