## 🚀 Урок 1: Введение во Flutter, Архитектура и Фундамент Виджетов

### 1. Что Такое Flutter и Его Архитектура

Flutter — это UI-инструментарий (UI Toolkit) от Google для создания красиво скомпилированных, нативно скомпилированных, мультиплатформенных приложений из единой кодовой базы.

#### А. Принципы Работы "Под Капотом"

В отличие от многих кросс-платформенных фреймворков, Flutter не использует **OEM-виджеты** (нативные виджеты операционной системы, как в React Native).

1. **Собственный Движок Рендеринга:** Flutter использует движок **Skia** (тот же, что и в Google Chrome), чтобы **сам рисовать** каждый пиксель на экране. Flutter предоставляет холст (`Canvas`) от Skia и управляет тем, что на нем рисуется.
2. **Движок Dart VM:** Код Dart компилируется:

- **JIT** (Just-In-Time) для **разработки** (обеспечивает Hot Reload).
- **AOT** (Ahead-Of-Time) для **продакшена** (компилируется в быстрый нативный код C/C++/Assembly).

3. **Flutter Framework:** Написан на Dart и содержит все основные библиотеки: виджеты, анимация, жесты и т.д.
4. **Flutter Engine:** Написан на C/C++ и отвечает за:

- Рендеринг с помощью Skia.
- Низкоуровневый ввод/вывод (I/O).
- Доступ к сервисам операционной системы.

> **Совет для собеседования:** Если вас спросят, как Flutter добивается высокой производительности, отвечайте: "Благодаря **AOT-компиляции** кода Dart в нативный код и использованию **собственного движка рендеринга Skia**, который рисует виджеты напрямую на графическом процессоре, минуя мост (Bridge) между JavaScript и нативным кодом."

### 2. Фундаментальная Триада Flutter

В Flutter все строится на трех ключевых деревьях, которые тесно связаны:

1. **Widget Tree (Дерево Виджетов):** Это то, что вы пишете. Это декларативные конфигурации (планы) для элементов UI.
2. **Element Tree (Дерево Элементов):** Это "живое" связующее звено. Каждый виджет создает один `Element`. `Element` содержит ссылку на Виджет и ссылку на `RenderObject`. `Element` управляет жизненным циклом (перестройкой) Виджета.
3. **RenderObject Tree (Дерево Рендеринга):** Это низкоуровневые объекты, которые занимаются фактической работой: расположением (layout), окрашиванием (painting) и hit-testing.

> **Как работает Hot Reload?** При изменении кода Dart, он обновляется в Dart VM (JIT). Flutter просто реконструирует **Дерево Виджетов**, а затем сравнивает его с существующим **Деревом Элементов**. Если конфигурация Виджета изменилась, `Element` обновляет `RenderObject`. Это очень быстрый процесс, поскольку Виджеты являются неизменяемыми (`const`) и легкими.

### 3. Классификация Виджетов

Все в Flutter — это виджеты. Их можно разделить на две категории:

#### А. Stateless Widgets (`StatelessWidget`)

- **Неизменяемые (Immutable):** Их конфигурация (поля) задается при создании и **не может быть изменена** в течение их жизненного цикла.
- **Не хранят состояние:** Не имеют внутренней памяти, зависят только от внешних данных.
- **Метод `build()`:** Единственный метод, который возвращает дерево других виджетов.

```dart
// Виджет, который отображает фиксированный текст
class WelcomeMessage extends StatelessWidget {
  final String title; // Конфигурация, установленная при создании

  const WelcomeMessage({super.key, required this.title});

  @override
  Widget build(BuildContext context) {
    return Text(
      title, // Отображаем переданный заголовок
      style: const TextStyle(fontSize: 24),
    );
  }
}

```

#### Б. Stateful Widgets (`StatefulWidget`)

- **Изменяемые (Mutable):** Нужны для виджетов, которые должны меняться в ответ на действия пользователя или внешние события (например, счетчик, чекбокс).
- **Состоят из двух частей:**

1. `StatefulWidget`: Сам виджет (неизменяемый).
2. `State` (Состояние): Отдельный класс, который хранит **изменяемые данные** и управляет логикой.

```dart
class CounterWidget extends StatefulWidget {
  const CounterWidget({super.key});

  @override
  State<CounterWidget> createState() => _CounterWidgetState();
}

class _CounterWidgetState extends State<CounterWidget> {
  // ЭТО ИЗМЕНЯЕМОЕ СОСТОЯНИЕ (State)
  int _counter = 0;

  void _incrementCounter() {
    // setState() - ключевой метод!
    // Он говорит Flutter: "Данные изменились, пожалуйста, перестрой (rebuild) виджет!"
    setState(() {
      _counter++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Счет: $_counter'),
        ElevatedButton(
          onPressed: _incrementCounter,
          child: const Text('Увеличить'),
        ),
      ],
    );
  }
}

```

### 4. Ключевые Методы Жизненного Цикла `State`

В классе `State` есть несколько важных методов, которые вызываются в определенные моменты:

| Метод                         | Когда вызывается                                                                              | Назначение                                                                         |
| ----------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| **`createState()`**           | Сразу после создания `StatefulWidget`.                                                        | Создает объект `State`.                                                            |
| **`initState()`**             | Вызывается **один раз** при создании объекта `State`.                                         | Идеально подходит для инициализации данных, подписок, контроллеров.                |
| **`didChangeDependencies()`** | Вызывается после `initState()` и при изменении зависимостей (например, `InheritedWidget`).    | Хорошее место для инициализации, зависящей от `BuildContext`.                      |
| **`build()`**                 | Вызывается при создании виджета, после `initState()`, и каждый раз после вызова `setState()`. | Определяет UI. **Должен быть чистым** (без побочных эффектов).                     |
| **`didUpdateWidget()`**       | Вызывается, когда родитель перестраивает виджет с новой конфигурацией.                        | Используется для реакции на изменение полей виджета (`widget.fieldName`).          |
| **`dispose()`**               | Вызывается, когда виджет навсегда удаляется из дерева.                                        | **Критически важно** для очистки ресурсов: отмена подписок, закрытие контроллеров. |

> **Профессиональный Совет:** Всегда используйте `const` для конструкторов `StatelessWidget` и `StatefulWidget` (если их поля неизменяемы). Это позволяет Flutter быть более эффективным при Hot Reload.

---

## 🚀 Ваше Задание на Урок 1

1. **Установите Flutter SDK** и убедитесь, что команда `flutter doctor` не выдает ошибок.
2. Создайте новый проект Flutter.
3. **Перепишите** стандартный пример с счетчиком (`_CounterWidgetState`) таким образом, чтобы:

- Класс `CounterWidget` принимал обязательный именованный параметр `String title` (Stateless часть).
- В `_CounterWidgetState`:
- Инициализируйте счетчик в `initState()` (например, 10), а не в объявлении.
- Добавьте в `build` `Text` с переданным `title`.
- В `dispose()`, выведите в консоль сообщение о том, что виджет удаляется (имитация очистки). (Вам нужно будет использовать `Navigator.push` или подобное, чтобы виджет был удален для проверки `dispose`, но пока достаточно просто добавить код.)

4. **Объясните**, почему счетчик (`_counter`) должен находиться в классе `State` (`_CounterWidgetState`), а не в `StatefulWidget` (`CounterWidget`).

После выполнения этого задания мы перейдем к **Уроку 2: Глубокое Понимание Контекста (`BuildContext`) и Работа с Assets**.
